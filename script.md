>>> 前向传播总览 #B08
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
请生成一张 16:9 横向专业教学信息图，4K 质感，深海军蓝渐变背景（#07111f 到 #10243a），叠加极淡青色工程网格和少量微弱粒子。画面采用干净的横向流程布局，内容占画布 85% 宽度，底部预留字幕安全区，不要在底部 18% 放重要文字。顶部居中主标题「前向传播：数据如何流过模型」，大号思源黑体，浅灰白 #E6EDF3。中间从左到右排列四个大型半透明圆角卡片，卡片填充 #0F2236，边框 #4FD1C5，轻微外发光，卡片之间用粗青色箭头连接。四个卡片依次是：1「嵌入」图标为字符 e 变成 16 格向量；2「注意力」图标为 Q/K/V 连线网络；3「前馈网络（MLP）」图标为 16→64→16 的堆叠条；4「输出 logits」图标为 27 个柱状分数。左侧入口小标签「输入字符」，右侧出口小标签「下一个字符的预测」。整体为高端科技教育风、扁平 2.5D 矢量插画，线条锐利，中文文字必须清晰正确，不要乱码，不要多余英文，不要摄影风。

--- narration ---
上一集我们说，模型就是 4000 多个随机数字。
这一集，我们把一段文字喂进去，看看这些数字是怎么被用起来的。
这个过程叫 **前向传播**，forward pass。
它把「输入字符」和「模型参数」组合起来。
最终输出一个预测：下一个字符是什么。
整个流程分四步：**嵌入**、**注意力**、**前馈网络**、输出。
我们一步一步来。


>>> 嵌入：把字符变成向量 #B09
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
请生成一张 16:9 横向专业矢量信息图，深海军蓝渐变背景（#0A1628 到 #1A2F4A），带极淡青色网格。顶部主标题「嵌入：把字符变成向量」，大号思源黑体，浅灰白。画面主体分成上、中、下三层：上层左侧是一个发光青色字符方块，内部写「e」，旁边小标签「token id = 4」。从字符方块分出两条虚线箭头到中层两张大表格。左表标题「wte：token 嵌入表」，画成 27×16 的矩阵抽象，一行用暖橙色 #F97316 高亮，并输出一条 16 格青色向量，标签「16 维 token 向量」。右表标题「wpe：位置嵌入表」，同样一行高亮，输出一条 16 格青色向量，标签「16 维位置向量」。下层中央用醒目的橙色加号把两条向量合并，输出一条更亮的 16 格向量，标签「嵌入向量 = 字符信息 + 位置信息」。布局要宽松但信息密度高，线条清晰，所有中文文字必须准确可读，不要乱码，不要多余装饰。

--- narration ---
你给模型一个字符，比如 e，它的 token id 是 4。
模型不能直接用这个 4。
4 和 5 之间没有天然的意义关系，它们只是查找表的下标。
模型需要把这个 id 翻译成一个 **向量**，一组数字。
代码里就两行：从 **wte** 这张 27 行 16 列的表里，取出第 4 行。
那是字符 e 的 16 维含义向量。
但只有「是哪个字符」还不够。
**abc** 和 **cba** 用的字符一样，意思完全不同。
所以我们再查第二张表 **wpe**，告诉模型这个字符在第几个位置。
位置 0 有它的 16 维向量，位置 1 有另一组，依此类推。
把「字符向量」和「位置向量」加起来，就是这一步要送进 Transformer 的输入。
这一个向量同时编码了 **是什么字符** 和 **在哪个位置**。


>>> Transformer Block 的整体结构 #B10
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
请生成一张 16:9 横向专业架构图，深海军蓝渐变背景，细微青色网格。画面中央是一个大型发光圆角容器，标题「Transformer Block」，占画布约 70% 宽度。容器内部采用从下到上的数据流，箭头清楚：最下方「输入 x」；然后「RMSNorm」浅紫色小模块；然后「多头注意力」青色大模块；然后橙色加号节点「+ 残差」；再到第二个「RMSNorm」；再到「前馈网络（MLP）」青色大模块；最后橙色加号节点「+ 残差」和顶部「输出」。在容器右侧画两条暖橙色虚线残差旁路，从输入绕过注意力汇入第一个加号，从中间状态绕过 MLP 汇入第二个加号。右侧留一个小注释框：「GPT 是多个 block 的堆叠」「microgpt 只用了 1 层」。使用统一的技术图标、清晰层级、浅灰白中文标签，文字必须正确可读，不要把模块顺序画反。底部留出50个像素左右的空间，不要有前景内容，用于后期加字幕。

--- narration ---
Transformer 由若干个相同的 block 堆叠而成。
GPT-4 有几十层。**microgpt** 只用了一层，但结构是一样的。
每一个 block 干两件事。
先做 **注意力**，再过一个小的 **前馈网络（MLP）**。
这两步前面各做一次归一化。
每一步之后，结果都会和这一步的输入相加。
这叫 **残差连接**，等会儿讲。
注意力是 Transformer 最关键的发明。
我们重点讲它。


>>> Q、K、V：注意力的三个角色 #B11
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
请生成一张 16:9 横向专业教学信息图，深海军蓝背景，淡青色网格。顶部主标题「Q、K、V：注意力的三个角色」。左侧放一个大型发光输入向量条，标签「输入向量 x」。从 x 分出三条粗箭头到右侧三列并行结构，每列包含一个矩阵、一个角色向量和一句短说明。第一列矩阵「wq」，输出「Query / Q」，说明「我要找什么」，颜色浅蓝；第二列矩阵「wk」，输出「Key / K」，说明「我能提供什么」，颜色青色 #4FD1C5；第三列矩阵「wv」，输出「Value / V」，说明「被关注时传出的内容」，颜色暖橙 #F97316。每个角色用 16 格向量条表示，矩阵用半透明网格方块表示。底部加小注释「wq、wk、wv 是模型要学习的参数」。布局三路对称、边距一致、文字清楚，避免卡通感，保持高端工程教学风。底部留出50个像素左右的空间，不要有前景内容，用于后期加字幕。

--- narration ---
注意力机制要解决的问题是：
**当前位置该从前面哪些字符里取信息？**
举个例子。模型看到 j-o-h-n，正在预测第 5 个字符。
它「回头看」前面的 j、o、h、n。
这四个字符里，哪个对预测最有帮助？
为此，模型给 **每个字符** 都算三个向量。
**Query**（查询）：我要找什么。
**Key**（键）：我能提供什么。
**Value**（值）：如果你看上我，我给你的内容。
这三个向量都从同一个输入向量出发。
分别乘以三个不同的矩阵 **wq**、**wk**、**wv**，得到三个向量。
这三个矩阵就是模型要学的参数。
训练完毕后，wq 学会了该提什么样的问题。
wk 学会了该亮什么样的旗子，wv 学会了该传什么样的消息。


>>> 打分与加权：注意力的核心运算 #B12
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
请生成一张 16:9 横向专业算法流程图，深海军蓝渐变背景，细微青色网格。顶部主标题「注意力核心：打分 → Softmax → 加权求和」。画面从左到右分三段。左段：上方一个醒目的「当前位置 Q」向量，下方四个「K1、K2、K3、K4」小向量，Q 到每个 K 用点积连线连接，连线旁显示分数 1.2、0.4、3.1、0.8，并标注「除以 √d 稳定数值」。中段：一个明亮的 Softmax 转换箭头，输出四段式权重条，权重为 0.10、0.04、0.78、0.08，其中 0.78 用暖橙色高亮。右段：四条 V 向量按权重汇入一个加权求和节点，输出「注意力输出」新向量。底部小结文字「相关性越高，拿到的信息越多」。整体像顶级在线课程的算法可视化，文字必须清晰，公式不要画错，不要拥挤。底部留出50个像素左右的空间，不要有前景内容，用于后期加字幕。

--- narration ---
有了 Q、K、V，注意力做这样一件事：
**当前位置的 Q，去和前面所有位置的 K 做点积。**
点积值高，说明这两个位置很匹配。低，说明不太相关。
代码里这一步算出来的叫 **attention logits**。
它是一组分数，每个位置一个。
算分时会除以一个常数，也就是根号 head_dim。
这样做是为了让数值不要爆炸，保持训练稳定。
得到一串分数后，用 **softmax** 把它变成一个概率分布。
这些概率加起来正好是 1。
然后用这个概率，对所有位置的 V 做 **加权求和**。
这就是注意力的输出。
当前位置融合了所有相关位置的信息，融合权重由相似度决定。
一句话总结：**注意力** 等于我先看谁和我有关。
然后按关系紧密程度，把他们的信息加权拿过来。


>>> 多头：让模型从多个角度看 #B13
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
请生成一张 16:9 横向专业信息图，深海军蓝渐变背景，极淡网格。顶部标题「多头注意力：从多个角度看」。画面主体是四条并行水平通道，占画布 80% 宽度，分别标记「头 1」「头 2」「头 3」「头 4」，四条通道用青色系不同明度区分。每条通道内部画一个简洁迷你流程：Q/K/V → 打分 → 加权 → 输出向量。每条通道左侧配一个短标签，分别是「看前一个字符」「看名字开头」「看辅音搭配」「看长度模式」，表示不同关注角度。右侧四条输出汇入一个大框「拼接 concat」，再经过矩阵「wo」，输出完整的 16 格向量。底部小字「microgpt 使用 4 个头，每头处理 4 维」。图形要整洁、层次强、专业课程风，中文文字准确无乱码。底部留出50个像素左右的空间，不要有前景内容，用于后期加字幕。

--- narration ---
刚才讲的是「一头」注意力。
实际中，我们让模型同时跑 **多头**——microgpt 是 4 头。
为什么要多头？
一头注意力可能学到「看前一个字符是什么」。
另一头可能学到「看名字开头是什么」。
再一头可能学到「看辅音元音的搭配」。
多头让模型在不同的子空间各看各的，最后把它们的结果 **拼接** 起来。
每一头处理的维度更小。
16 维分给 4 头，每头 4 维。
但关注的角度不同，加起来比单头更丰富。
最后再过一个矩阵 **wo**。
把多头的拼接结果再混合一次，作为整个注意力模块的输出。


>>> 残差连接与 RMSNorm #B14
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
请生成一张 16:9 横向专业教学对比图，深海军蓝渐变背景，细微青色网格。顶部主标题「让训练稳定的两个技巧」。画面左右两栏，中间用细分隔线。左栏标题「残差连接」，画一个从下到上的数据流：输入 x 进入「注意力」模块，同时一条暖橙色旁路绕过模块，汇入上方加号节点，输出「x + 注意力(x)」。旁边用短注释写「保留原始信号」「缓解梯度衰减」。右栏标题「RMSNorm」，画一条不规则、亮暗不一的向量经过「RMSNorm」模块后，变成规整、亮度稳定的向量，旁边短注释「方向基本保留」「幅度进入稳定范围」。整体使用清晰矢量线条、半透明模块、统一中文字体，画面专业、克制、适合技术教学。底部留出50个像素左右的空间，不要有前景内容，用于后期加字幕。

--- narration ---
注意力之后有两个细节，不讲清楚就会看不懂。
第一个是 **残差连接**：注意力的输出要加回它的输入。
代码里就是 x 等于对应元素相加的那种写法。
为什么？模型很深的时候，信号一层层往下传容易丢失。
反向传播时，梯度也容易衰减消失。
残差连接让原始信号有一条「短路」通道，可以直接传到后面。
这是深层网络能训得动的关键技巧。
第二个是 **RMSNorm**。
在每一步注意力和 MLP **之前**，先把向量归一化。
保持方向不变，但把幅度规整到一个稳定的范围。
它防止训练过程中数值越变越大或越变越小。
这两个技巧本身不增加模型表达能力。
但没它们，深层模型几乎训不出来。


>>> MLP：非线性与维度变换 #B15
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
请生成一张 16:9 横向专业教学信息图，深色科技背景 #0D1117，淡青色网格。顶部大标题「前馈网络（MLP）：加入非线性」。中间是一条清晰的横向流程，占画布 85% 宽度：左侧「16 维输入向量」用 16 格青色向量表示；箭头到中间大模块「线性扩展」，旁边标注「16 → 64」；再箭头到明亮胶囊模块「ReLU 激活」，用橙色高亮表示非线性拐点；再箭头到右侧模块「线性压回」，标注「64 → 16」；最终输出「16 维输出向量」。底部用两条短说明：「MLP 给模型增加非线性表达能力」「只做线性变换，学不到复杂规律」。整体简洁高级、类似专业深度学习课程幻灯片，所有中文和数字必须清楚正确。底部留出50个像素左右的空间，不要有前景内容，用于后期加字幕。

--- narration ---
注意力之后，每个 block 还有一个小的前馈网络。
也就是 **MLP**。
先把 16 维放大到 64 维。
过一个 **ReLU** 激活函数，再压回 16 维。
它的作用是给模型增加 **非线性** 表达能力。
光做线性变换是学不到复杂规律的。


>>> 输出：lm_head 与 logits #B16
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
请生成一张 16:9 横向专业教学信息图，深海军蓝渐变背景，淡青色网格。顶部标题「输出：模型对下一个字符的打分」。左侧是一个 16 格发光向量条，标签「最后一层输出：16 维」。中间是一个大型矩阵模块，标签「lm_head」，下方小字「映射到 27 个候选字符」，用箭头表示矩阵乘法。右侧是一组横向柱状图或条形图，表示 27 个 logits，标签包含 a、b、c、…、z、BOS。让字母「o」对应的柱子最高，用暖橙色 #F97316 高亮，旁边标注「当前最高分」。其他柱子用青色，长度参差。底部小结「logit 是未归一化分数，最高分就是模型的猜测」。整体像高质量算法课程图，避免杂乱，文字清晰准确。底部留出50个像素左右的空间，不要有前景内容，用于后期加字幕。

--- narration ---
走完所有 block，我们得到一个 16 维的向量。
最后一步，乘上 **lm_head** 矩阵，把它映射成 **27 个数**。
对应 27 个候选字符（26 个字母加上 BOS）。
这 27 个数叫 **logits**。
谁的值最高，模型就觉得下一个字符最可能是它。


>>> Loss：交叉熵告诉模型错了多少 #B17
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
请生成一张 16:9 横向专业教学信息图，深海军蓝渐变背景，微弱网格。顶部主标题「Cross Entropy：用一个数字表示模型错了多少」。画面左侧是「Softmax 后的概率分布」柱状图，包含多个候选字符柱子，其中正确答案「m」用暖橙色 #F97316 高亮，并标注「正确答案」。中间是清晰的计算节点「-log(p正确)」，从橙色柱子引线进入该节点。右侧输出一个大数字卡片「loss = 0.42」，浅灰白大字。画面下方放两个小对比卡片：左卡「p正确 = 0.9 → loss 小」，使用绿色对勾；右卡「p正确 = 0.001 → loss 大」，使用橙色警示图标。整体要像专业机器学习课程幻灯片，公式和中文必须准确清晰，不要画成复杂数学黑板。底部留出50个像素左右的空间，不要有前景内容，用于后期加字幕。

--- narration ---
但模型刚开始是随机的，它的预测大概率是错的。
我们怎么用一个数字告诉它错了多少？
答案是 **cross entropy loss**，交叉熵损失。
具体做法：把 logits 过一次 **softmax** 变成概率分布。
然后取出 **正确答案那个字符的概率**，对它取负对数。
如果模型给正确答案分配了 0.9 的概率，loss 很小。
如果它给正确答案分配了 0.001 的概率，loss 就非常大。
整个序列的 loss 是每个位置 loss 的平均。
这个数字，就是模型本次考试的量化成绩。
**loss** 越小，模型越接近真相。


>>> 收束与下集预告 #B18
@enter: fade
@exit: fade
@visual: image

--- visual ---
请生成一张 16:9 横向专业课程收束页，深色背景 #0D1117，轻微青色网格和柔和光晕。画面中央是一个完整流程链路，占画布 85% 宽度：左到右依次为「嵌入」「注意力」「MLP」「logits」「loss」，每个节点是半透明圆角卡片，用青色箭头连接，最后的「loss」节点用暖橙色高亮。顶部大标题「一次前向传播走完了」，浅灰白大字。流程下方放一句副标题「loss 只是数字，参数怎么调？」。右下角放小型预告卡片「下一集：反向传播与 Adam」，使用青色边框。整体克制、现代、专业，有结束感和下一集悬念；中文文字必须清楚准确，不要出现多余英文。底部留出50个像素左右的空间，不要有前景内容，用于后期加字幕。

--- narration ---
我们走完了一次完整的前向传播。
从一个字符开始，经过嵌入、注意力、MLP、输出，最终算出一个 **loss**。
它告诉模型：这次考得有多差。
但 loss 只是一个数字。
模型怎么根据这个数字，去 **调整** 自己的 4000 多个参数？
下一集，我们讲 **反向传播**。
以及让 **Adam** 这个优化器，把 loss 真正变成模型能力的提升。
