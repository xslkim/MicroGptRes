>>> 进阶篇：揭秘 AI 编程的真相 #B21
@enter: zoom-in
@exit: fade
@visual: image

--- visual ---
横屏 16:9 章节标题卡，分辨率 1920x1080。

整体风格：电影级标题卡、强排版冲击力、对比叙事、极简科技感。

背景：深色 #0d1117，左半带极淡透视网格 #1a2332，中心柔和蓝色环境光晕。

主构图：画面左右分区，左占 55%、右占 40%，中间留 5% 呼吸空间。

**左半（垂直居中，距左 100px）：**
- 顶部小标签 "PART 03 · 进阶篇"，等宽字体 32px，浅灰 #8b949e，字间距加宽。
- 主标题 "揭秘 AI 编程的真相"，字号 110px，粗体白 #e6edf3，左对齐，分两行排版可读性更好。
- 主标题带轻微蓝色外发光，不刺眼。
- 副标题 "Agent 核心只有 20 行，剩下都是工程"，字号 40px，颜色 #8b949e，"20 行" 用蓝色 #58a6ff 高亮。
- 副标题下方一条 6px 粗的蓝色发光横线，宽 360px，左对齐。

**右半（垂直居中，距右 100px）：**
- 巨大对比数字组：上方 "20" 字号 280px 粗体白色，旁边 "行" 字号 64px 浅灰。
- 中央一根 6px 蓝色横线分隔，宽 220px。
- 下方 "100,000+" 字号 120px，颜色 #f85149，旁边 "行工程代码" 字号 36px 浅灰，整组带轻微划除装饰线。
- 数字下方小注 "工程冰山藏在水下"，字号 26px，#8b949e。

**底部带状区（距底 70px）：**
- 四个圆角胶囊横向排列，间距 32px，总宽占画布 70%，居中。
- 标签内容："上下文管理"、"MCP 协议"、"多 Agent 协作"、"动手实现 20 行"。
- 每个胶囊：深灰 #161b22，1px 边框 #30363d，文字字号 28px，颜色 #8b949e。

光效：标题轻微呼吸光；横线带从左到右流光；右侧数字组有微微脉冲。

避免：卡通人物、复杂插画、可读代码、大面积高饱和色。

--- narration ---
AI 写代码看着像魔法
但拆开来看
其实就是一个 **while 循环**
真正的 Agent 核心代码
**不到 20 行**
那为什么 Claude Code 仍然让人觉得神奇？
答案是 **工程**
我们这就拆穿
但先把基础铺一下
没看过前两集的朋友
30 秒补回来


>>> 30 秒补完前两集 #B22
@enter: fade
@exit: fade
@visual: image

--- visual ---
横屏 16:9 紧凑回顾图，分辨率 1920x1080。

整体风格：教科书级清晰、信息密度可控、强调"5 件东西 + 1 个循环"的心智模型。

背景：深色 #0d1117，左上角有非常淡的蓝色渐变光，画面中心有微弱光晕。

**画面顶部（距顶 60px 跨整幅居中）：**
- 主标题 "30 秒补完前两集"，字号 64px，粗体白色 #e6edf3。
- 副标题 "Agent = 5 件东西 + 1 个循环"，字号 32px，颜色 #8b949e，"5 件东西"、"1 个循环" 用蓝色 #58a6ff 高亮。

**画面主体（垂直居中）：5 张组件卡片横向排列**
- 5 张卡片等距，间距 30px，总宽占画布 82% 居中，每张宽 290px、高 360px。
- 每张卡片样式：圆角 18px，背景 #161b22，1px 边框 #30363d，左侧 6px 发光边条。
- 每张卡片内部分三段（上中下）：
  - 上段（高 130px）：大图标居中，尺寸 90px。
  - 中段（高 80px）：标题字号 34px，粗体白色 #e6edf3，居中。
  - 下段（高 110px，内边距 24px）：一句描述字号 24px，浅灰 #8b949e，居中两行。
- 5 张卡片（左→右）颜色 + 图标 + 标题 + 描述：
  - ① 边条金 #d29922，图标 📜，标题 "System Prompt"，描述 "Agent 的说明书 告诉它怎么做事"。
  - ② 边条蓝 #58a6ff，图标 🛠，标题 "工具"，描述 "LLM 的双手 能读写文件、跑命令"。
  - ③ 边条紫 #9d7aff，图标 📚，标题 "消息数组"，描述 "Agent 的记忆 装着完整对话"。
  - ④ 边条蓝 #1f6feb，图标 🧠，标题 "LLM"，描述 "无状态思考引擎 不记任何事"。
  - ⑤ 边条橙 #f0883e，图标 🔄，标题 "while 循环"，描述 "把上面 4 件串起来 不断驱动"。

**外围循环箭头（包裹 5 张卡片）：**
- 一根粗 8px 蓝色 #58a6ff 发光弧形箭头，从画面下方左侧出发，弯过卡片底部、右侧、顶部，回到左侧形成"环抱"姿态。
- 箭头线条带蓝色光带流动效果。
- 箭头顶部居中位置标签 "Agent 循环驱动一切"，字号 28px，颜色 #58a6ff，蓝色发光。

**画面底部（距底 60px 跨整幅居中）：**
- 一行小字回顾 "前两集案例：写 Hello World · 生成架构文档 · 自动修 Bug"，字号 26px，浅灰 #8b949e，"Hello World"、"架构文档"、"自动修 Bug" 用 #58a6ff 高亮。

光效：5 张卡片依次淡入；外围箭头流光环绕；底部小字静态。

避免：过多文字、复杂连线、品牌图标、可读代码、繁琐符号。

--- narration ---
**LLM 本质是无状态函数**
给一段输入 返回一段输出
它没有记忆
那 Agent 的"记忆"哪里来？
答案是 **消息数组**
每次调用都发送完整对话
配上 **System Prompt** 和工具定义
LLM 决定调工具还是回答
工具结果再追加进数组
整个流程套进 **while 循环**
就是 Agent
前两集我们用它跑过三个场景
写代码、生成架构文档、自动修 Bug


>>> 上下文窗口的天花板 #B23
@enter: fade
@exit: fade
@visual: image

--- visual ---
横屏 16:9 概念示意图，分辨率 1920x1080。

整体风格：信息图隐喻，玻璃容器即将溢出。

背景：深色 #0d1117，干净无杂物，中心有淡蓝色环境光。

主构图：画面分左右两区，玻璃容器在左，对比数据卡在右。

**左区（画布占 50%）：**
- 中央一个高大透明玻璃容器，宽度 360px，高度 800px，垂直居中。
- 容器边框为蓝色 #58a6ff，线宽 5px，带柔和外发光。
- 容器顶部明显容量上限刻度线，刻度右上方标注 "约 200K tokens"，字号 34px，颜色 #d29922。
- 容器内部由多层彩色横条填充到 92% 高度。
  - 绿色横条代表 user，蓝色横条代表 assistant，紫色横条代表 tool_result（呼应前情提要中"消息数组"的三色）。
  - 越靠顶部横条越密集，形成"快满了"的压迫感。
- 容器上方有几张半透明消息卡片正在往下落，被顶部容量线挡住。
- 顶部右侧红色警告图标 "!"，颜色 #f85149，带轻微闪烁感。
- 容器左下角小标签 "= Agent 的全部记忆"，字号 22px，浅灰 #8b949e。

**右区（画布占 45%，垂直居中，距右 100px）：**
- 顶部小标签 "Context Window"，字号 32px，#8b949e，等宽字体。
- 主标题 "上下文窗口"，字号 84px，粗体白 #e6edf3，左对齐。
- 主标题下方蓝色横线 6px，宽 220px。
- 三张对比数据卡片纵向堆叠，间距 24px，宽度 100%，每张高 130px，背景 #161b22，1px 边框 #30363d，圆角 16px，内边距 28px。
  - 卡片 ①："200K tokens" 字号 64px 粗体白 + 右上小标 "上限"（金色 #d29922 字号 24px）。
  - 卡片 ②："≈ 50 万中文字" 字号 38px 白色 + 右上小标 "感受一下"（浅灰 22px）。
  - 卡片 ③："读完几十个源文件就满" 字号 32px 白色 + 右上小标 "现实情况"（浅灰 22px）。
- 底部大字 "超过上限怎么办？"，字号 50px，粗体白色，"怎么办" 用蓝色 #58a6ff 高亮。

光效：玻璃容器细腻反光，内部消息层有轻微发光但不花哨；警告图标脉冲。

避免：真实水杯、照片质感、过多文字、小字号密集说明。

--- narration ---
第一个秘密是 **上下文窗口**
LLM 能处理的文本量有上限
常见上限约 **200K tokens**
听起来很多
换算成中文大约 50 万字
但分析一个真实项目
读几十个源文件就可能撑满
**消息数组就是这个容器**
不能无限增长
超过上限会发生什么？


>>> 大项目装不下怎么办 #B24
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
横屏 16:9 策略对比图，分辨率 1920x1080。

整体风格：教学级对比图，强反差，左右红绿叙事。

背景：深色 #0d1117，画面中心一根纵向虚线 #30363d 把画面分成两半，中央一个金色 "VS" 圆形徽章（直径 90px，边框 #d29922 字号 36px 粗体白居中）。

**画面顶部（距顶 60px，跨整幅画布居中）：**
- 大标题 "大项目装不下，怎么办？"，字号 70px，粗体白色。
- 副标题 "答案：从不一次性加载全部"，字号 32px，#8b949e。

**左半（错误做法，画布 0%–48%）：**
- 区块小标题 "❌ 朴素做法：全部塞进来"，字号 38px，颜色 #f85149，左对齐距左 80px、距顶 200px。
- 主视觉：左侧一个项目目录树缩略图（约 30 个文件节点，灰色细线连接，整体宽 280px、高 360px），右侧一个粗大蓝色箭头指向一个发胀的玻璃容器（同 B23 风格，但顶部裂开，红色裂纹）。
- 容器内部彩色横条溢出顶部，红色 token 文字飘散溢出。
- 容器右侧大字 "💥 OVERFLOW"，字号 50px，颜色 #f85149，发光。
- 区块底部小注 "刚读到第 8 个文件就爆了"，字号 26px，浅灰。

**右半（正确做法，画布 52%–100%）：**
- 区块小标题 "✅ Agent 做法：按需检索"，字号 38px，颜色 #3fb950，左对齐距区块左 40px、距顶 200px。
- 主视觉：纵向 4 步流程，每步圆角胶囊宽度占该半区 90%、高 95px，间距 28px，背景 #161b22，左侧 6px 蓝色边条 #58a6ff，圆角 14px，内边距 24px。
  - 胶囊 ①：图标 🗂 + 等宽 26px 蓝色 `Glob "**/*.py"` + 右侧浅灰 22px "→ 文件清单 200 字"。
  - 胶囊 ②：图标 🔎 + 等宽 26px 蓝色 `Grep "class.*Model"` + 右侧浅灰 22px "→ 锁定 3 个关键文件"。
  - 胶囊 ③：图标 📖 + 等宽 26px 蓝色 `Read main.py:1-100` + 右侧浅灰 22px "→ 只读入口和核心片段"。
  - 胶囊 ④：图标 🧠 + 等宽 26px 蓝色 `Subagent: 探索 train/` + 右侧浅灰 22px "→ 回传 500 字摘要"。
- 区块底部一根横向上下文用量进度条，宽度 100%、高 24px，绿色填充约 12%，标签 "上下文用量：12%"，字号 24px 浅灰。

**画面底部（距底 50px 跨整幅居中）：**
- 一行总结 "原则：先看目录 → 再定位关键 → 最后精读"，字号 32px，白色，"先看目录"、"定位关键"、"精读" 用蓝色 #58a6ff 高亮。

光效：左侧容器溢出处红色脉冲；右侧每个步骤胶囊依次蓝色发光；底部进度条绿色流光。

避免：拥挤小字、卡通插画、繁琐说明、过多图标。

--- narration ---
真实项目动辄几十万行代码
上下文绝对装不下
那 Claude Code 怎么生成架构文档？
**它根本不一次性全读**
而是像人类工程师一样
先看全局 再深入细节
比如生成一个开源项目的架构文档
它先用 **Glob** 列出文件清单
再用 **Grep** 定位关键的类
然后只 **Read** 入口和核心片段
甚至派 **子 Agent** 读完一个模块
子 Agent 只回传一段摘要
真正进入主上下文的
是经过过滤的 **精华**
不是项目原文


>>> 上下文压缩：满了再压 #B25
@enter: fade
@exit: fade
@visual: image

--- visual ---
横屏 16:9 横向漏斗图，分辨率 1920x1080。

整体风格：专业信息图、层次清楚、强调"逐层压缩"。

背景：深色 #0d1117，左上有轻微蓝色渐变光。

**画面顶部（距顶 50px，左对齐距左 100px）：**
- 主标题 "上下文压缩的五层策略"，字号 60px，粗体白色 #e6edf3。
- 副标题 "再小心也会满，满了就压缩"，字号 30px，#8b949e。

**主构图（垂直居中，画布宽 12%–88%）：**
- 横向漏斗，从左（最宽）到右（最窄），高度从画布 60% 缩到 30%，整体高度占画布约 50%。
- 漏斗由 5 块圆角梯形卡片左右连接而成，每块宽约画布 14%，颜色由浅到深：
  - 第 1 层（最宽）：浅蓝 #58a6ff，剪刀图标（白色 60px 居中），下方文字 "裁剪冗余输出"，字号 32px 白色粗体。
  - 第 2 层：蓝色 #3b82d6，压缩图标，文字 "微压缩对话"，字号 32px。
  - 第 3 层：深蓝 #2f6fab，折叠图标，文字 "折叠早期上下文"，字号 30px。
  - 第 4 层：更深蓝 #255a8a，摘要文档图标，文字 "自动摘要"，字号 32px。
  - 第 5 层（最窄）：暗蓝 #1a3a5c，脑内存储图标，文字 "跨会话记忆"，字号 28px。
- 每块卡片底部小标签 "更激进 →"，字号 20px，浅灰。
- 漏斗中央有一条从左到右的发光箭头，颜色从 #58a6ff 渐变到 #d29922，宽 6px。
- 箭头上方小字 "信息逐层浓缩"，字号 26px，#8b949e。

**画面右下区域（距右 80px、距底 120px）：**
- 一段竖排引言卡片，背景 #161b22 圆角 16px，宽 360px、高 200px，内边距 24px：
  - 顶部小标签 "💡 类比"，金色字号 22px。
  - 主文字 "像一个 智能遗忘系统"，字号 30px，白色，"智能遗忘" 用 #58a6ff 高亮。
  - 下方文字 "优先保留 最近 + 最重要"，字号 24px，浅灰，"最近"、"最重要" 用蓝色高亮。

**画面底部（距底 50px 跨整幅居中）：**
- 一行强调 "前面省着用 + 这里再压缩 = 长会话不爆"，字号 28px，浅灰，"长会话不爆" 用蓝色高亮。

光效：漏斗五层依次蓝色脉冲；中央箭头流光。

避免：拥挤小字、复杂 3D、真实机器、强烈红色警告。

--- narration ---
即便有按需检索
长会话最终还是会满
这时 Claude Code 启动 **五层压缩**
第一层裁剪工具输出的冗余
比如超长日志只保留关键
第二层微压缩不重要的轮次
第三层折叠最早期的上下文
第四层对整段对话做摘要
第五层把关键存为 **跨会话记忆**
每一层都比上一层更激进
像一个智能的遗忘系统
优先保留最近和最重要的信息


>>> MCP：AI 工具的标准接口 #B26
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
横屏 16:9 架构连接图，分辨率 1920x1080。

整体风格：现代技术架构图，重点突出 MCP 是统一连接层。

背景：深色 #0d1117，带极淡网格线 #1a2332。

主构图：纵向三层结构，上、中、下，每层横向铺开。

**画面顶部（距顶 50px 跨整幅居中）：**
- 大标题 "MCP：AI 工具的标准接口"，字号 60px，粗体白色 #e6edf3。
- 副标题 "工具是 LLM 的双手 — MCP 是这双手的接口标准"，字号 28px，颜色 #8b949e，"双手"、"接口标准" 用 #58a6ff 高亮。

**顶层 — AI 应用层（距顶 200px）：**
- 横向排列 4 张应用卡片，等距，间距 60px，总宽占画布 86% 居中。
- 每张卡片宽 250px、高 110px，圆角 16px，背景 #161b22，1px 边框 #30363d，文字白色 30px 粗体居中。
- 卡片标签：Claude Code / Cursor / Copilot / Windsurf。
- 每张卡片底部有 1 条 2px 蓝色发光线向下延伸，汇聚到中层 MCP 节点。

**中层 — MCP 协议层（垂直居中）：**
- 画面正中是一个大型蓝色 USB-C 接口图标，宽 320px、高 140px，颜色 #58a6ff，强烈发光。
- 图标下方主标题 "MCP"，字号 96px，粗体白色 #e6edf3，居中。
- 副标题 "Model Context Protocol"，字号 32px，颜色 #8b949e，居中。
- 接口左侧醒目标注 "AI 的 USB-C"，字号 36px，颜色 #58a6ff，带细箭头指向接口。

**底层 — 外部服务层（距底 100px）：**
- 横向排列 6 张服务图标卡片，等距，间距 36px，总宽占画布 88% 居中。
- 每张宽 200px、高 110px，深灰 #161b22 圆角 14px，1px 边框 #30363d，图标白色 50px 居上，文字字号 24px 居下。
- 6 个标签：Database / File System / GitHub / Slack / Browser / Docs。
- 每张顶部有 1 条 2px 发光线向上汇聚到中央 MCP 接口。

**画面右上角（距右 80px、距顶 60px）：**
- 注释卡片 "一个协议 = N × M 连接"，字号 26px 白色，深色背景 #161b22 圆角胶囊，内边距 18px。

光效：所有连接线都有从外向 MCP 流动的蓝色光点，表现"统一接入"。

避免：真实品牌 Logo 过度细节、彩色杂乱、协议细节文字。

--- narration ---
第二个秘密是 **MCP**
全称 Model Context Protocol
还记得工具是 LLM 的双手吗
**MCP 是这双手的接口标准**
你可以把它理解为
AI 工具世界的 **USB-C**
任何支持 MCP 的 AI 应用
都能接入任何实现 MCP 的服务
数据库、文件系统、GitHub
Slack、浏览器都能挂上去
不用每个工具都重写适配层
**一个协议** 统一连接方式


>>> 多 Agent 与后台 Agent #B27
@enter: fade
@exit: fade
@visual: image

--- visual ---
横屏 16:9 双模块架构图，分辨率 1920x1080。

整体风格：清晰技术流程图，左右两块展示"协作"和"异步"。

背景：深色 #0d1117，画面中央细虚线 #30363d 把画面纵向分成左右两半。

**画面顶部（距顶 50px 跨整幅居中）：**
- 大标题 "多 Agent + 后台 Agent"，字号 60px，粗体白色。
- 副标题 "拆任务 + 异步执行"，字号 30px，#8b949e，"异步执行" 用 #58a6ff 高亮。

**左半 — 多 Agent 协作（画布 0%–48%）：**
- 区块标题 "多 Agent 协作"，字号 42px，粗体白色，左对齐距左 80px、距顶 180px。
- 上部正中：一个主 Agent 节点，圆角矩形 280×120，蓝色发光边框 #58a6ff，背景 #161b22，文字 "主 Agent" 字号 34px 粗体白居中。
- 下部三条蓝色发光连线（2px）分出，连接横排三个子 Agent 节点。
- 三个子 Agent 节点：每个 220×100，间距 28px，圆角矩形，背景 #161b22 边框 #30363d，文字 "子 Agent 1 / 2 / 3" 字号 26px 白色居中，距顶部主 Agent 约 200px。
- 每个子 Agent 节点旁（右下角）有一个迷你上下文窗口图标（像 B23 的玻璃容器，缩到 50×80px），内部有彩色消息层，象征 **独立的消息数组**。
- 三条绿色 #3fb950 回传箭头从子 Agent 顶部弯回主 Agent 底部，箭头标签 "结果汇总" 字号 22px 绿色。
- 区块底部小注 "每个子 Agent 上下文独立 — 主上下文不被污染"，字号 22px，浅灰，距底 90px。

**右半 — 后台 Agent（画布 52%–100%）：**
- 区块标题 "后台 Agent"，字号 42px，粗体白色，左对齐距区块左 40px、距顶 180px。
- 主体是一条横向 5 节点流程线，垂直居中，节点间距 30px：
  ① GitHub Issue 图标卡 → ② 云端 Agent 节点（云朵造型，边框 #58a6ff，内部小机器人图标 + "云端自主" 22px）→ ③ 代码改动卡 → ④ 测试结果卡 → ⑤ Pull Request 图标卡。
- 每个节点宽 130px、高 110px，深灰圆角，文字 22px 白色居中，节点之间用 2px 蓝色细线连接，箭头蓝色 #58a6ff。
- 流程线上方小字标注 "无需盯着聊天窗口"，字号 24px，浅灰。
- 区块底部小注 "提个 Issue → 等 PR 通知"，字号 22px，浅灰，距底 90px。

**画面底部（距底 40px 跨整幅居中）：**
- 强调文字 "更大的任务，靠 拆分 + 异步 完成"，字号 32px 白色，"拆分"、"异步" 用 #58a6ff 高亮。

光效：主 Agent 蓝色脉冲；云端 Agent 蓝色流动光晕；回传箭头绿色光带。

避免：拟人化机器人脸、品牌 Logo、复杂小字。

--- narration ---
第三个秘密是 **多 Agent**
主 Agent 把任务拆分
派给多个子 Agent 并行执行
**每个子 Agent 都有独立的消息数组**
完成后向主 Agent 汇报
这也是处理大项目的杀手锏
子 Agent 读完一个模块
只把摘要回传主 Agent
主上下文不会被污染
进一步就是 **后台 Agent**
你在 GitHub 提一个 Issue
Agent 在云端自动工作
写代码、跑测试、提 PR
你只需要做 Code Review
这已经不是未来
而是正在发生的现实


>>> 动手实现：20 行核心 Agent #B28
@enter: fade-up
@exit: fade
@visual: animation

--- visual ---
横屏 16:9 代码 + 注解双栏图（动画），分辨率 1920x1080。
全屏深色背景 #0d1117。

**画面顶部（距顶 60px 跨整幅居中）：**
- 主标题 "动手实现：20 行核心 Agent"，字号 64px，粗体白色 #e6edf3，"20 行" 用蓝色 #58a6ff 高亮。
- 副标题 "理解了原理，自己写一个不难"，字号 28px，颜色 #8b949e。

主体区域分为左右两列，距顶 200px、距底 130px，左列宽占画布 44%、右列宽占画布 44%，中间留 4% 间距。

**左列 — 代码块**（距左 80px，宽约 800px）：
- 背景 #161b22，圆角 18px，1px 边框 #30363d，内边距 36px。
- 等宽字体（JetBrains Mono），字号 30px，行高 1.55，语法高亮。
- 代码内容（Python 伪代码）：

```python
tools = [read_file, write_file, bash]
messages = [{"role": "user",
             "content": user_input}]

while True:
    response = call_llm(
        system_prompt,
        tools,
        messages,
    )
    messages.append(response)

    if response.has_tool_calls:
        for tc in response.tool_calls:
            result = execute(tc)
            messages.append(result)
    else:
        print(response.text)
        break
```

**右列 — 6 张注解卡片**（距右 80px，宽约 800px）：
- 每张卡片宽 100%、高 95px，间距 18px，纵向堆叠。
- 卡片样式：背景 #161b22，圆角 14px，左侧 6px 蓝色发光边条 #58a6ff，1px 边框 #30363d，内边距 24px。
- 卡片内左右两栏：左侧大圆形编号 ①~⑥（直径 60px，蓝色边框，内部数字 #58a6ff 字号 36px 粗体）；右侧上行标题（白色字号 28px 粗体）+ 下行简述（浅灰 #8b949e 字号 22px）。
- 6 张卡片内容：
  - ①「定义工具列表」+ "可被 LLM 调用的能力清单"
  - ②「初始化记忆」+ "消息数组就是 Agent 的状态"
  - ③「调用 LLM」+ "传入 system + tools + messages"
  - ④「追加 LLM 回复」+ "把响应记录进消息数组"
  - ⑤「执行工具调用」+ "结果再次追加进数组"
  - ⑥「输出并退出」+ "纯文本响应即结束循环"

**连接虚线**：
- 6 张注解卡片左侧编号圆，与左列对应代码行之间，用细虚线 #58a6ff（1px，30% 透明度）连接：
  - ① ↔ 第 1 行（tools = ...）
  - ② ↔ 第 2-3 行（messages = ...）
  - ③ ↔ 第 5-9 行（response = call_llm(...)）
  - ④ ↔ 第 10 行（messages.append(response)）
  - ⑤ ↔ 第 12-15 行（for tc / execute / append）
  - ⑥ ↔ 第 16-18 行（else / print / break）

**动画时序：**
[0s] 代码块整体淡入 + 标题淡入。
[1.5s] 注解卡片从右侧依次淡入，间隔 0.3s（6 张共 1.8s 完成）。
[3.5s] 连接虚线依次绘制，从代码行向注解卡片端点延伸，间隔 0.2s。
[5.5s] 整体保持静止；标题"20 行"轻微脉冲 1 次。

**画面底部（距底 50px 跨整幅居中）：**
- 一行小字 "总长不到 20 行 — 但还需要权限、压缩、错误恢复 ..."，字号 24px，浅灰。

避免：颜色杂乱、注解字号过小、连接虚线交叉混乱、3D 效果。

--- narration ---
理解了原理
自己实现也不难
核心就是一个 **while 循环**
首先定义可用的工具列表
初始化消息数组
然后循环调用 LLM
把回复追加到消息数组
如果回复包含工具调用
就执行工具并把结果追加
如果是普通文本就输出结束
**不到二十行代码**
你就有了一个能用的 Agent


>>> 从玩具到产品：工程冰山 #B29
@enter: fade
@exit: fade
@visual: image

--- visual ---
横屏 16:9 冰山隐喻图，分辨率 1920x1080。

整体风格：戏剧化但克制的工程复杂度示意，强对比叙事。

背景：上半部分深色 #0d1117 + 极淡冷蓝色雾气；下半部分深海渐变 #07111f。

主构图：画面分左右两区，左区是冰山主图，右区是文字与数字对比。

**左区 — 冰山主图（画布 0%–55%）：**
- 一座垂直冰山，宽度约 600px，高度占画布 78%，水平居中于左区。
- 水面线水平横贯左区，位置在画布顶 38% 处，蓝色 #58a6ff 发光线 6px。
- 水面以上（约 18% 高度）：
  - 小冰山顶部蓝色发光，标注 "Agent 循环"，字号 38px，粗体白色 #e6edf3。
  - 顶部旁边有一个小型 while 循环图标（环形箭头，蓝色 #58a6ff，60px）。
  - 顶部小注 "约 20 行核心"，字号 24px，浅灰。
- 水面以下（约 60% 高度）：
  - 巨大冰山主体被分成 6 层横向区域，每层都有标签：
    - ① "权限与安全控制"
    - ② "上下文压缩管理"
    - ③ "工具路由与协议"
    - ④ "错误恢复与重试"
    - ⑤ "并发与性能优化"
    - ⑥ "持久化与跨会话记忆"
  - 每层文字字号 26px，白色，左侧配小图标，分隔线淡蓝 #1f6feb。
  - 越往下颜色越深，体现工程复杂度。

**右区 — 文字与对比数字（画布 60%–100%，垂直居中，距右 100px）：**
- 顶部小标签 "ICEBERG"，字号 30px，浅灰 #8b949e，等宽字体。
- 主标题 "核心是起点 工程是产品"，字号 64px，粗体白色 #e6edf3，左对齐。
- 主标题下蓝色横线 6px，宽 220px。
- 下方一组对比数字卡片：
  - 卡片 1：上方 "20" 字号 100px 粗体白色 + 旁边 "行 · 核心循环"，字号 28px，浅灰。
  - 卡片 2：上方 "100,000+" 字号 100px 粗体红色 #f85149 + 旁边 "行 · 工程系统"，字号 28px，浅灰。
- 底部一行小字 "公开拆解估算：核心很小，工程占绝大多数"，字号 22px，浅灰。

光效：水面有蓝色发光线；冰山边缘有冷光；右侧"100,000+" 数字有微微红色脉冲。

避免：卡通冰山、夸张海浪、可读小字、过度装饰。

--- narration ---
但从玩具到产品
差距非常大
有公开拆解估算
**核心循环只占很小一部分**
绝大多数都是工程代码
权限控制、安全检查
上下文压缩、错误恢复
并发优化、工具路由
这些才是真正的挑战
但理解了核心原理
你只是看到了 **冰山一角**
你已经有了 **正确的起点**


>>> 总结：三个核心洞察 #B30
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
横屏 16:9 总结卡片，分辨率 1920x1080。

整体风格：高质量课程结尾页，强层级，强记忆点，极简深色 UI。

背景：深色 #0d1117，中心非常淡的蓝色光晕，背景稀疏代码符号水印（极低对比度）。

**画面顶部（距顶 60px 跨整幅居中）：**
- 大标题 "三个核心洞察"，字号 70px，粗体白色 #e6edf3。
- 副标题 "理解这三点 = 理解 AI 编程的本质"，字号 30px，颜色 #8b949e。

**主体区域 — 三张洞察卡片（垂直居中）：**
- 三张卡片横向排列，等距，间距 48px，总宽占画布 88% 居中。
- 每张卡片宽 540px、高 480px，背景 #161b22，圆角 24px，顶部 8px 蓝色发光边条 #58a6ff，1px 边框 #30363d。
- 卡片内布局上下三段：
  - 上段（占卡片 50%）：巨大编号 "01" / "02" / "03"，字号 140px，颜色 #58a6ff，等宽粗体，居中。
  - 中段：分隔横线 4px，宽 100px，蓝色，居中。
  - 下段：洞察主文字两行，字号 32px，白色粗体，居中，行高 1.3。
- 三张卡片内容：
  - 01："LLM 无状态"（第一行）+ "用消息数组模拟记忆"（第二行）
  - 02："工具调用"（第一行）+ "让 LLM 操作真实世界"（第二行）
  - 03："Agent 循环"（第一行）+ "while + API + 工具执行"（第二行）

**画面下方（三张卡片下方距 32px）：**
- 一行小字 "完整原理 → Part 1     真实场景追踪 → Part 2"，字号 24px，浅灰 #8b949e，居中，"Part 1"、"Part 2" 用 #58a6ff 高亮。

**画面底部（距底 60px 跨整幅居中）：**
- 一个蓝色发光 CTA 按钮 "现在 你也能写一个 Agent"，字号 34px，白色粗体，宽 720px、高 96px，圆角 48px，背景 #1f6feb，外发光 #58a6ff。

光效：三张卡片依次淡入，编号有轻微蓝色脉冲；CTA 按钮蓝色呼吸光。

避免：励志海报风、复杂插画、过多装饰、小字号。

--- narration ---
最后总结三个核心洞察
第一 **LLM 是无状态的**
Agent 通过消息数组模拟记忆
每次调用都发送完整历史
第二 **工具调用** 是关键突破
让只会说话的 LLM
能操作文件、运行命令
第三 **Agent 循环** 是核心引擎
本质就是 while 加 API 加工具
理解了这三点
你就真正理解了 AI 编程
现在 去动手实现一个吧
