>>> 进阶篇：完整图景 #B21
@enter: zoom-in
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 章节标题卡，分辨率 1080x1920。

整体风格：电影级标题卡、极简科技感、深色背景、强排版冲击力。

背景：深色 #0d1117，带非常淡的透视网格纹理（#1a2332），中心有一圈柔和蓝色环境光晕。

主构图：画面垂直居中，内容区域占画布约 80% 宽度。

**主标题：**
- 中央大字"进阶篇"，字号 132px，粗体白色 #e6edf3，字间距略加宽。
- 标题带非常轻微的蓝色外发光，不要刺眼。

**副标题：**
- 主标题下方 44px 显示"现代 AI 编程的完整图景"，字号 42px，颜色 #58a6ff，居中。
- 副标题下方一条 6px 粗的蓝色发光横线，宽度约画布 46%，居中。

**四个主题预告：**
- 横线下方 120px，四个小标签两行两列排布，总宽占画布 78%。
- 标签文字："上下文管理"、"MCP 协议"、"多 Agent"、"动手实现"。
- 每个标签是深灰圆角胶囊（#161b22），边框 #30363d，文字字号 28px，颜色 #8b949e。

光效：标题轻微呼吸光，横线带从左到右的流光效果。

避免：卡通人物、复杂插画、可读代码、大面积高饱和色。

--- narration ---
原理讲清楚了
但真实的 AI 编程工具
还要解决很多工程问题
这一段我们聊四个进阶话题


>>> 上下文窗口的天花板 #B22
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 概念示意图，分辨率 1080x1920。

整体风格：清晰的信息图隐喻，像一只即将溢出的玻璃容器。

背景：深色 #0d1117，干净无杂物，中心有淡蓝色竖向光晕。

主构图：

**上下文窗口容器：**
- 画面中央放置一个高大的透明玻璃容器，宽度占画布 58%，高度占画布 62%。
- 容器边框为蓝色 #58a6ff，线宽 5px，带柔和外发光；顶部有明显的容量上限刻度线。
- 容器顶部标签"Context Window"，字号 34px，白色粗体。
- 顶部刻度右侧标注"约 200K tokens"，字号 30px，颜色 #d29922。

**消息层堆叠：**
- 容器内部由多层彩色横条填充到 92% 高度。
- 绿色横条代表 user，蓝色横条代表 assistant，紫色横条代表 tool_result。
- 越靠顶部横条越密集，形成"快满了"的压迫感。

**溢出与拦截：**
- 容器上方还有几张半透明消息卡片正在往下落，但被顶部容量线挡住。
- 顶部右侧显示红色警告图标"!"，颜色 #f85149，带轻微闪烁感。

**底部问题：**
- 容器下方 90px 显示大字"超过上限怎么办？"，字号 46px，粗体白色。
- "怎么办？"用蓝色 #58a6ff 高亮。

光效：玻璃容器有细腻反光，内部消息层有轻微发光但不花哨。

避免：真实水杯、照片质感、过多文字、小字号密集说明。

--- narration ---
第一个问题是上下文窗口
LLM 能处理的文本量有上限
常见上限可到约 **200K tokens**
听起来很多
但分析一个大项目时
读几十个文件就可能撑满
消息数组不能无限增长
超过上限怎么办？


>>> 上下文压缩 #B23
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 分层漏斗图，分辨率 1080x1920。

整体风格：专业信息图、层次清楚、强调"逐层压缩"。

背景：深色 #0d1117，顶部到中部有轻微蓝色渐变光。

主构图：画面中央是一座 5 层压缩漏斗，从上到下逐渐变窄，高度占画布 68%，宽度从 86% 缩到 42%。

**五层结构：**
- 第 1 层（最宽）：浅蓝 #58a6ff，剪刀图标，文字"裁剪冗余输出"，字号 34px。
- 第 2 层：蓝色 #3b82d6，压缩图标，文字"微压缩对话"，字号 34px。
- 第 3 层：深蓝 #2f6fab，折叠图标，文字"折叠早期上下文"，字号 34px。
- 第 4 层：更深蓝 #255a8a，摘要文档图标，文字"自动摘要"，字号 34px。
- 第 5 层（最窄）：暗蓝 #1a3a5c，脑内存储图标，文字"跨会话记忆"，字号 32px。

每层都是圆角梯形卡片，白色文字，图标在左、文字在右，内边距充足。

**流动箭头：**
- 漏斗中央有一条从上到下的发光箭头，颜色从 #58a6ff 渐变到 #d29922。
- 箭头旁边每层之间显示小字"更浓缩"，字号 22px，颜色 #8b949e。

**侧边注释：**
- 右侧竖排注释"信息逐层浓缩，像一个智能遗忘系统"，字号 26px，白色。
- "智能遗忘系统"用 #58a6ff 高亮。

画面顶部标题"上下文压缩的五层策略"，字号 48px，白色粗体，居中。

避免：拥挤小字、复杂 3D、真实机器、强烈红色警告。

--- narration ---
Claude Code 用五层压缩策略
第一层裁剪工具输出的冗余部分
比如超长的日志只保留关键信息
第二层微压缩不太重要的对话轮次
第三层折叠最早期的上下文
第四层对整段对话做摘要
第五层把关键信息存为跨会话记忆
每一层都比上一层更激进
像一个智能的遗忘系统
优先保留最近和最重要的信息


>>> MCP：AI 工具的标准接口 #B24
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 架构连接图，分辨率 1080x1920。

整体风格：现代技术架构图，重点突出 MCP 是统一连接层。

背景：深色 #0d1117，带极淡网格线 #1a2332。

主构图：上下两组节点，中间是一个巨大的 USB-C 连接器隐喻。

**顶部：AI 应用层**
- 顶部区域横向排列 3 个应用卡片，总宽占画布 86%。
- 卡片标签分别为"Claude Code"、"Cursor"、"Copilot"。
- 每张卡片是深灰 #161b22 圆角矩形，边框 #30363d，文字白色字号 28px。
- 卡片下方各有一条发光线连接到中间 MCP。

**中间：MCP 协议层**
- 画面正中是一个大型蓝色 USB-C 接口图标，宽度约画布 34%，颜色 #58a6ff，带强发光。
- 图标下方大标题"MCP"，字号 78px，粗体白色。
- 副标题"Model Context Protocol"，字号 28px，颜色 #8b949e。
- 旁边有醒目标注"AI 的 USB-C 接口"，字号 34px，颜色 #58a6ff，带箭头指向接口。

**底部：外部服务层**
- 底部区域两行三列排列 6 个服务图标卡片：Database、File System、GitHub、Slack、Browser、Docs。
- 每张卡片字号 24px，图标白色，边框深灰。
- 每张卡片向上连线汇聚到 MCP 接口。

光效：所有连接线都有从外向 MCP 流动的蓝色光点，表现"统一接入"。

避免：真实品牌 Logo 过度细节、彩色杂乱、复杂协议细节。

--- narration ---
第二个话题是 **MCP**
全称 Model Context Protocol
你可以把它理解为 AI 的 USB-C
一个统一的工具连接协议
支持 MCP 的 AI 应用
可以连接实现了 MCP 的外部服务
数据库、文件系统、GitHub
浏览器、Slack 都能通过 Server 接入
不用每个应用都重写适配层
一个协议统一连接方式


>>> 多 Agent 与后台 Agent #B25
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 双模块架构图，分辨率 1080x1920。

整体风格：清晰的技术流程图，分上下两块展示"并行协作"和"云端后台执行"。

背景：深色 #0d1117，模块之间用细虚线 #30363d 分隔。

**顶部模块：多 Agent 协作**
- 模块标题"多 Agent 协作"，字号 42px，白色粗体，左对齐，距左 64px。
- 中央上方是一个主 Agent 节点，圆角矩形，蓝色发光边框，文字"主 Agent"，字号 30px。
- 主 Agent 向下分出三条蓝色发光连线，连接三个子 Agent 节点。
- 三个子 Agent 横向排列，分别标注"子 Agent 1"、"子 Agent 2"、"子 Agent 3"，字号 24px。
- 每个子 Agent 旁边都有一个小型上下文窗口容器，里面有不同颜色消息层，表示"独立上下文"。
- 三条绿色回传箭头从子 Agent 回到主 Agent，标签"结果汇总"，字号 22px。

**底部模块：后台 Agent**
- 模块标题"后台 Agent"，字号 42px，白色粗体，左对齐。
- 中央是一个云朵容器，边框 #58a6ff，内部有小机器人图标和文字"云端自主工作"，字号 26px。
- 从左到右是一条流程线：
  GitHub Issue 图标 → 云端 Agent → 代码改动卡片 → 测试结果卡片 → Pull Request 图标。
- 每个节点是深灰圆角卡片，宽度约 150px，高度 110px，文字字号 22px。
- 流程线上方标注"无需持续盯着聊天窗口"，字号 26px，颜色 #8b949e。

画面底部强调文字"更大的任务，靠拆分和异步执行完成"，字号 32px，白色，"异步执行"用蓝色高亮。

光效：主 Agent 和云端 Agent 有蓝色脉冲光；结果回传箭头用绿色 #3fb950。

避免：拟人化机器人脸、大量品牌 Logo、复杂小字。

--- narration ---
更强大的是多 Agent 架构
主 Agent 把任务拆分
派给多个子 Agent 并行执行
每个子 Agent 有独立上下文
完成后向主 Agent 汇报
进一步就是 **后台 Agent**
你在 GitHub 提一个 Issue
Agent 在云端自动工作
写代码、跑测试、提 PR
你只需要做 Code Review
这已经不是未来
而是正在发生的现实


>>> 动手实现：最小 Agent #B26
@enter: fade-up
@exit: fade
@visual: animation

--- visual ---
全屏深色背景 (#0d1117)，竖屏 9:16 布局。
顶部居中标题 "最小 Agent 伪代码"，字号 56px，粗体白色，距顶部 80px。
标题下方 40px 处出现一个代码块区域，宽度占画布 90%，背景 #161b22，圆角 16px，内边距 24px。
代码使用等宽字体，字号 28px，行高 1.5。

[0s] 代码块淡入，显示以下 Python 伪代码（语法高亮）：

```python
tools = [read_file, write_file, bash]
messages = [{"role":"user", "content": 用户输入}]

while True:
    response = call_llm(
        system_prompt,
        tools,
        messages
    )
    messages.append(response)

    if response.has_tool_calls:
        for tool_call in response.tool_calls:
            result = execute(tool_call)
            messages.append(result)
    else:
        print(response.text)
        break
```

[2s] 右侧逐行出现注释标注（字号 22px，颜色 #58a6ff），用细线指向对应代码行：
第1行旁标注 "① 定义工具"
第2行旁标注 "② 初始化记忆"
第4-8行旁标注 "③ 调用 LLM"
第9行旁标注 "④ 追加到记忆"
第11-13行旁标注 "⑤ 执行工具"
第15行旁标注 "⑥ 输出结果"

--- narration ---
理解了原理
自己实现也不难
核心就是一个 while 循环
首先定义可用的工具列表
初始化消息数组
然后循环调用 LLM
把 LLM 的回复追加到消息数组
如果回复包含工具调用
就执行工具并把结果追加
如果是普通文本就输出给用户
不到二十行代码
你就有了一个能用的 Agent


>>> 从玩具到产品 #B27
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 冰山隐喻图，分辨率 1080x1920。

整体风格：戏剧化但清晰的工程复杂度示意图。

背景：深色 #0d1117，上半部分有冷蓝色雾气，下半部分像深海渐变 #07111f。

主构图：画面中央是一座冰山，宽度占画布 78%，高度占画布 72%。水面线位于画面中上部，约在 38% 高度处。

**水面以上（可见 20%）：**
- 小块冰山顶部发蓝光，标注"Agent 循环"，字号 36px，白色粗体。
- 旁边有一个简单 while 循环图标，颜色 #58a6ff。
- 小注"核心逻辑"，字号 24px，颜色 #8b949e。

**水面以下（隐藏 80%）：**
- 巨大的冰山主体被分成 6 层横向区域，每层都有标签：
  "权限与安全控制"、"上下文压缩管理"、"错误恢复与重试"、"并发与性能优化"、"工具路由与协议"、"持久化与记忆"。
- 每层文字字号 28px，白色，左侧配小图标，边界线淡蓝 #1f6feb。
- 越往下颜色越深，体现工程复杂度。

**底部结论：**
- 画面底部大字"核心只是起点，产品难在工程"，字号 38px，白色粗体。
- 下方小字"一些公开拆解估算：核心循环很小，工程系统占绝大多数"，字号 24px，颜色 #8b949e。

光效：水面有一条蓝色发光线，冰山边缘有冷光，整体保持专业克制。

避免：卡通冰山、夸张海浪、难以阅读的小字。

--- narration ---
但从玩具到产品
差距非常大
有公开拆解估算
核心 Agent 循环只占很小一部分
绝大多数都是工程代码
权限控制、安全检查
上下文压缩、错误恢复
并发优化、工具路由
这些才是真正的挑战
但理解了核心原理
你就有了正确的起点


>>> 总结：三个核心洞察 #B28
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 总结卡片，分辨率 1080x1920。

整体风格：高质量课程结尾页，强层级、强记忆点、极简深色 UI。

背景：深色 #0d1117，中心有非常淡的蓝色光晕，背景上有稀疏代码符号水印。

画面顶部：
- 大标题"三个核心洞察"，字号 58px，白色粗体，居中，距顶 80px。
- 副标题"理解这些，就理解了 AI 编程的本质"，字号 28px，颜色 #8b949e。

主体区域：三张大型洞察卡片纵向堆叠，总高度占画布约 62%，宽度占 88%，居中，卡片间距 42px。

**卡片样式：**
- 背景 #161b22，圆角 20px，左侧 10px 蓝色发光边条。
- 左侧是巨大编号 "01" / "02" / "03"，字号 70px，颜色 #58a6ff，等宽粗体。
- 右侧是洞察文字，字号 34px，白色，行高 1.25。

**三张卡片内容：**
- 01："LLM 无状态，Agent 用消息数组模拟状态"
- 02："工具调用让 LLM 操作真实世界"
- 03："Agent 循环 = while + API + 工具执行"

画面底部：
- 一个蓝色发光按钮样式标签"现在你可以自己实现一个 Agent"，字号 30px，白色，宽度占画布 78%，高度 84px，圆角 42px。

光效：三张卡片依次淡入，编号有轻微蓝色脉冲。

避免：励志海报风、复杂插画、过多装饰、小字号。

--- narration ---
最后总结三个核心洞察
第一 **LLM 是无状态的**
Agent 通过消息数组模拟状态
每次调用都发送完整历史
第二 **工具调用**是关键突破
让只会说话的 LLM
能操作文件、运行命令
第三 **Agent 循环**是核心引擎
本质就是 while 加 API 加工具
理解了这三点
你就真正理解了 AI 编程
去动手实现一个吧
