>>> 场景一：写 C++ Hello World #B09
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 场景标题卡，分辨率 1080x1920。

整体风格：电影级标题卡、极简但有视觉冲击、科技感与编程美学结合。

背景：深色（#0d1117），中心有一个非常微弱的蓝色辐射光晕。

主构图：

**画面上半部分（约占 50%）：**
- 居中放置一个大型的 C++ Logo（"C++"字符设计，粗体），尺寸约 480x480 像素。
- Logo 颜色：蓝色（#58a6ff）发光，带霓虹外光效，玻璃质感。
- Logo 周围环绕几个小的代码符号装饰（{}、();、//），灰色（#30363d），点缀分布。

**画面中部：**
- 一行小标签"SCENE 01"（浅灰 #8b949e，字号 28px，等宽字体，字间距宽）。
- 下方主标题"用 C++ 写 Hello World"（粗体白色 #e6edf3，字号 56px，居中）。
- 标题下方一条短横线（3px 蓝色 #58a6ff，宽度 200px，居中）。

**画面下半部分：**
- 一个迷你终端窗口模拟（圆角矩形，深灰背景 #161b22，宽度占 70%，高度 240px），左上角三个 macOS 风格小圆点（红黄绿）。
- 终端内容（视觉模拟，无需可读）：可见 `$ ` 提示符，下面一段彩色的命令行轨迹（橙色关键字、绿色字符串、白色普通文本），还有一个闪烁的青色光标。
- 终端窗口下方淡灰小字"等待 AI 编写..."（#6e7681 字号 24px）。

光效：C++ Logo 有蓝色脉冲光晕；终端有微弱的环境光。

色彩：黑底 + 蓝色主调 + 白色文字 + 灰色辅助。

避免：复杂插画、卡通、过度装饰。

--- narration ---
第一个场景
用 **Claude Code** 写一个 C++ 程序
用户只说了一句话
"用 C++ 写个 hello world 并运行"
我们来追踪每一次 LLM 调用
看看到底发生了什么


>>> 第一次调用：生成代码 #B10
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 API 调用解析图，分辨率 1080x1920。

整体风格：技术蓝图、清晰展示输入输出、信息密度适中、专业感。

背景：深色（#0d1117），无装饰。

构图：画面纵向分为两个主要区域。

**画面顶部（标题区）：**
- 大字"API Call #1"（蓝色 #58a6ff，字号 48px，等宽字体，粗体），居中。
- 副标题"第一次 LLM 调用"（白色字号 30px）。
- 顶部装饰：左右两侧延伸出短横线（#30363d）。

**画面上半部分（输入区）：**
- 区域标签"📥 输入"（白色字号 36px，粗体，左对齐，距左边距 60px）。
- 区域内三个堆叠的小卡片，从上到下：

  **卡片 A — System Prompt：**
  - 深棕黄色背景（#3d2c0e），左侧金色边条（#d29922）。
  - 简化呈现：折叠状/省略号显示"...几千 token..."（金色字号 28px）。
  - 标签"System Prompt"（金色字号 24px 左上角）。
  - 卡片高度约 100px，宽度占 85%。

  **卡片 B — Tools 定义：**
  - 深蓝背景（#1f2d3d），左侧蓝色边条（#58a6ff）。
  - 内显示三个微缩工具图标 + 名称："read_file"、"write_file"、"bash"（白色字号 22px）。
  - 标签"工具列表"。
  - 高度约 100px。

  **卡片 C — 用户消息：**
  - 绿色背景（#1d3829），左侧绿色边条（#3fb950）。
  - 文字内容"用 C++ 写个 hello world 并运行"（白色字号 32px，粗体）。
  - 标签"User"。
  - 高度约 120px。

**画面中部：**
- 一个发光的 LLM 处理盒子（圆角矩形，蓝色边框 + 内部显示大脑图标），尺寸约 200x140，居中。
- 上方箭头从输入区指向 LLM 盒子。
- 下方箭头从 LLM 盒子指向输出区。
- 箭头标签"思考 ↓"（蓝色字号 24px，等宽字体）。

**画面下半部分（输出区）：**
- 区域标签"📤 输出"（白色字号 36px，粗体，左对齐）。
- 一个突出的输出卡片：

  **tool_use 卡片：**
  - 蓝色发光边框（#58a6ff），背景深色（#161b22），圆角 16px。
  - 卡片头部："assistant → tool_use"（蓝色标签字号 24px）。
  - 卡片内容（结构化 JSON 风格视觉）：
    - 第一行：`name: "write_file"`（关键字蓝色，字符串绿色，等宽字体字号 26px）
    - 第二行：`path: "hello.cpp"`
    - 第三行：`content: "..." `（带省略号）
    - 第四行下方一个微缩的 C++ 代码片段预览（深色框 + 几行彩色伪代码：可见 `#include`、`int main`、`cout` 等关键字高亮颜色）。
  - 卡片高度约 320px，宽度占 88%。

光效：LLM 盒子有蓝色脉冲光；tool_use 卡片有蓝色外发光强调。

色彩：黑底 + 三色区分（金 System Prompt、蓝 Tools、绿 User）+ 蓝色 LLM 输出。

避免：3D 效果、照片质感。

--- narration ---
第一次 API 调用
输入包含三样东西
几千 token 的 **System Prompt**
工具定义列表
和用户的请求
LLM 分析后决定先写文件
输出了一个 **write_file** 工具调用
里面是一段 JSON 格式的指令
包含文件路径和完整 C++ 代码
注意 LLM 自己没有写任何文件
它只是输出了一段文本
说"请帮我写这个文件"


>>> 第二次调用：发现编译器 #B11
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 API 调用图 + 推理示意，分辨率 1080x1920。

整体风格：技术示意图，重点突出"LLM 如何推断出环境信息"。

背景：深色（#0d1117）。

构图：

**画面顶部（标题）：**
- "API Call #2"（蓝色字号 48px 粗体）。
- 副标题"它怎么知道用 g++ 编译？"（白色字号 28px）。

**画面上半部分（输入区简化呈现）：**
- 一个折叠的消息堆栈卡片（深灰 #161b22，圆角 16px），左侧有 3 条不同颜色的横线代表 3 条消息：绿（user）、蓝（assistant tool_use）、紫（tool_result）。
- 卡片右上角小标签"消息数组：3 条"（白色字号 24px）。
- 卡片下方追加一条金色细标签"+ System Prompt（独立参数）"（金色字号 22px），强调它不算进消息数组。

**画面中部（核心：推理来源）：**
重点展示三个并列的"信息来源" — 三个圆形发光节点，水平排列，连接到一个中心的"LLM 推断"标签：

- **节点 1（左）：** 文档图标（金色），下方标签"System Prompt 中的环境信息"（白色字号 22px）。其下小注："当前 OS / Shell / 工作目录"（浅灰字号 20px 等宽字体）。
- **节点 2（中）：** 大脑图标（紫色发光），下方标签"模型预训练知识"（白色字号 22px）。其下小注："C++ 常用 g++ / clang++ 编译"。
- **节点 3（右）：** 用户消息图标（绿色），下方标签"用户上下文"。其下小注："用户提到 C++"。

三个节点用细线汇聚到中心一个发光的灯泡图标（#d29922 黄色），灯泡下方文字"推断：尝试 g++"（粗体白色字号 30px）。

**画面下半部分（输出 tool_use）：**
- 一个 tool_use 卡片（同 B10 风格）：
  - 标签"assistant → tool_use"
  - `name: "bash"` （蓝色高亮）
  - `command: "g++ hello.cpp -o hello"` （字符串绿色高亮，等宽字体字号 26px）
  - 卡片底部小注："如果失败 → LLM 看到错误 → 换方案（试 clang）"（黄色字号 22px，斜体）。

**画面底部：**
一行强调文字"AI 不是无所不知 它在不断试错 + 推理"（白色字号 30px，重点字"试错"和"推理"用蓝色 #58a6ff 高亮）。

光效：灯泡图标有黄色脉冲光晕；中心推断箭头有汇聚效果。

色彩：黑底 + 多色信息源区分 + 黄色作为"推断"的视觉焦点。

--- narration ---
文件写入成功
结果追加到消息数组
现在数组里有 3 条消息
第二次调用 LLM
LLM 看到文件已写好
决定调用 **bash** 工具来编译
但它怎么知道用 g++ 呢
答案有三个来源
**System Prompt** 里有环境信息
比如当前系统、Shell 和工作目录
模型预训练知识告诉它
C++ 常用 g++ 或 clang++ 编译
所以它先选一个方案试
如果失败就换方案再试


>>> 第三次调用：编译并运行 #B12
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 双调用并排展示图，分辨率 1080x1920。

整体风格：连续动作的过程图、强调消息数组的累积过程。

背景：深色（#0d1117）。

构图：画面纵向分为两个等高区块，分别展示 Call #3 和 Call #4。

**顶部区块 — Call #3（编译成功 → 运行）：**
- 区块标题"API Call #3"（蓝色字号 38px，粗体），左对齐。
- 左侧：消息堆栈卡片（深灰背景 + 5 条不同颜色横线，标签"消息数组：5 条"）。
- 中间：箭头 → LLM 盒子 → 箭头。
- 右侧：tool_use 输出卡片，内容：
  - `name: "bash"`
  - `command: "./hello"`（绿色字符串高亮，等宽字号 24px）
- 区块底部：一个绿色对勾"✓ 编译成功"（#3fb950 字号 28px）。

**中部分隔线：**
一条横向的细虚线（#30363d），中间有一个向下的箭头标记"↓"。

**底部区块 — Call #4（运行成功 → 输出最终回复）：**
- 区块标题"API Call #4"（蓝色字号 38px）。
- 左侧：消息堆栈卡片（7 条横线，标签"消息数组：7 条"）。
- 中间：箭头 → LLM → 箭头。
- 右侧：输出是文本回复卡片（白色边框，深色背景），内容："程序运行成功！输出了 Hello, World!"（白色字号 24px）。
- 区块底部小标签"text response（无更多工具调用）"（浅灰字号 22px）。

**画面右侧（贯穿两个区块的纵向数据可视化）：**
一根纵向的进度条（细长矩形），从顶部到底部分成 8 段，每段对应一条消息（金、绿、蓝、紫、蓝、紫、蓝、紫颜色顺序）。条形图旁标签"消息数组生长过程"（白色字号 22px，旋转 -90 度）。

**画面顶部：**
小标题"完成最后两步"（浅灰字号 32px，居中）。

**画面底部：**
强调总结文字"4 次 LLM 调用 = 完整任务"（白色字号 36px，粗体；数字"4"用蓝色 #58a6ff 强调放大）。

光效：消息堆栈有微弱的发光感；绿色对勾有成功光晕；最终文本卡片有柔和强调光。

色彩：黑底 + 蓝色主调 + 绿色（成功）+ 多色消息条。

--- narration ---
编译成功
结果追加到数组
第三次调用
LLM 决定运行程序
执行 ./hello
输出 Hello World
第四次调用
LLM 看到运行成功
不再调用任何工具
直接输出文字总结给用户
任务完成
一共 4 次 LLM 调用
第四次调用前已有 7 条消息
最终回复后增长到 8 条


>>> 消息数组全貌 #B13
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 完整消息数组时间轴可视化，分辨率 1080x1920。

整体风格：清晰的列表/时间轴信息图、聚焦展示消息数组结构。

背景：纯净深色（#0d1117），无装饰。

构图：画面纵向是一条从上到下的消息时间轴。

**画面顶部（标题区）：**
- 大字标题"完整的消息数组"（白色字号 50px 粗体，居中），距顶 60px。
- 副标题"4 次调用后的最终状态"（浅灰 #8b949e 字号 28px）。

**主体（时间轴消息列表）：**

最左侧有一根纵向时间轴线（细线 #30363d，1px），从顶部贯穿到底部，每条消息处有一个圆形节点。

时间轴右侧依次排列 8 条消息卡片，每张卡片宽度占 80%，高度约 130px，左侧有彩色边条标识角色，圆角 16px。

每张卡片内容布局：左上角 role 标签（小字号 22px）+ 主体内容（字号 26px）+ 右上角序号（圆形徽章）。

**8 张卡片从上到下：**

1. **绿色边条 — user：**"用 C++ 写个 hello world 并运行"（绿色 #3fb950）

2. **蓝色边条 — assistant (tool_use)：**"🔧 write_file → hello.cpp"（蓝色 #58a6ff）

3. **紫色边条 — tool_result：**"✓ 文件已写入"（紫色 #9d7aff）

4. **蓝色边条 — assistant (tool_use)：**"🔧 bash → g++ hello.cpp -o hello"

5. **紫色边条 — tool_result：**"✓ 编译成功"

6. **蓝色边条 — assistant (tool_use)：**"🔧 bash → ./hello"

7. **紫色边条 — tool_result：**"Hello, World!"

8. **蓝色边条 — assistant (text)：**"程序运行成功！"

**画面右侧（贯穿全部卡片的注释）：**
一根从顶部到底部的弯曲花括号 `}`（白色 #e6edf3，粗体），花括号外侧标注大字"每次 API 调用都发送整个数组"（白色字号 32px，粗体；"整个数组"用蓝色 #58a6ff 高亮，文字旋转 -90 度沿纵向排列）。

**画面顶部右上角：**
一个小说明卡片"+ system prompt（独立参数）"（金色文字 #d29922 字号 22px，深色背景圆角矩形）。

**画面底部：**
一行小字总结"这就是'记忆'的全部真相 — 没有黑魔法"（浅灰 #8b949e 字号 28px，居中，斜体）。

光效：每张卡片有微妙投影；蓝色助手消息有柔光；紫色结果消息略暗。

色彩：黑底 + 三色清晰区分（绿用户、蓝助手、紫结果）+ 金色系统提示标签。

避免：照片质感、过度装饰。

--- narration ---
看一下完整的消息数组
8 条消息按顺序排列
绿色是用户输入
蓝色是助手输出
紫色是工具结果
**还有 System Prompt**
作为独立参数附加在每次调用上
**每次 LLM 调用**
**都发送这整个数组**
第四次调用时
LLM 读到了所有历史操作
才知道任务已经完成
这就是"记忆"的全部真相
没有任何黑魔法


>>> 场景二：分析 nanochat 项目 #B14
@enter: slide-left
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 场景过渡卡，分辨率 1080x1920。

整体风格：开源项目卡片风、电影级标题美学。

背景：深色（#0d1117）。

构图：

**画面顶部（小标签）：**
"SCENE 02"（浅灰 #8b949e 字号 28px 等宽字体，居中）。

**画面上半部分：**
- 一个大型的 GitHub 仓库卡片（圆角矩形，深灰背景 #161b22，宽度占 88%，高度 380px），居中。
- 卡片顶部：GitHub Octocat 图标（白色），右侧仓库名"karpathy / nanochat"（白色字号 38px 粗体；"karpathy /"是浅灰，"nanochat"是白色高亮）。
- 卡片中间：仓库描述"一个轻量级的聊天模型训练框架"（浅灰字号 24px）。
- 卡片底部：标签栏 — "★ 12.5k stars"、"Python"（蓝色小标签）、"MIT License"（绿色小标签），字号 22px。
- 卡片背景纹理：极淡的代码符号水印。

**画面中部（任务描述）：**
- 一个箭头从仓库卡片向下指向一个文档输出图标。
- 中间文字"目标：生成项目架构文档"（白色字号 32px，居中）。

**画面下半部分：**
- 一个空白文档卡片（虚线边框 #30363d），上面有"架构文档"占位文字（浅灰字号 28px），右侧一个小问号图标"?"（黄色 #d29922），暗示"待生成"。

**画面底部：**
强调文字"Claude Code 一开始没有本地项目上下文"（白色字号 30px，粗体；"没有本地项目上下文"用红色 #f85149 高亮）。

光效：GitHub 卡片有微妙发光；问号图标有轻微脉冲。

色彩：黑底 + GitHub 风的中性灰 + 强调色。

--- narration ---
第二个场景更复杂
让 Claude Code 分析开源项目
并生成架构文档
我们以 **nanochat** 为例
这是 Karpathy 的聊天模型项目
Claude Code 一开始没有本地项目上下文
它要靠工具一步步读懂


>>> 探索阶段：大量工具调用 #B15
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 流程瀑布图，分辨率 1080x1920。

整体风格：动态流程瀑布、信息密度适中、突出"多次工具调用累积上下文"的过程。

背景：深色（#0d1117）。

构图：画面左侧一根纵向粗线（"探索时间线"，蓝色发光 #58a6ff），从上到下贯穿。线上有多个等距节点（圆形），每个节点对应一次工具调用。线右侧排列对应的工具调用卡片，从上到下依次。

**画面顶部：**
- 标题"探索阶段：信息收集"（白色字号 44px 粗体，居中）。
- 副标题"十几次工具调用，逐步建立理解"（浅灰字号 26px）。

**主体瀑布（从上到下 7 张工具调用卡片）：**

每张卡片宽度占 75%（左侧给时间轴让位），高度约 110px，圆角 12px，深灰背景（#161b22），左侧蓝色边条（#58a6ff）。

每张卡片内：
- 左上：工具图标 + 工具名（蓝色字号 26px 等宽）
- 主体：参数预览（白色字号 22px 等宽）
- 右下：返回结果摘要（绿色或浅灰字号 20px）

1. **`bash: ls -la`** → "返回 12 个文件"
2. **`read_file: README.md`** → "返回 200 行说明"
3. **`read_file: train.py`** → "返回 300 行训练代码"
4. **`grep: "import"`** → "找到 47 处依赖"
5. **`read_file: model.py`** → "返回 180 行模型定义"
6. **`read_file: config.py`** → "返回配置参数"
7. **`grep: "class.*Model"`** → "找到 3 个模型类"

每张卡片末尾后面跟着一个小的"⊕"符号（绿色），象征追加到消息数组。

**画面右侧（贯穿全部的可视化）：**
一根从上到下的"消息数组容量条"（细长进度条），随着工具调用次数增加，进度条填充越来越满，从蓝色（#58a6ff）逐渐变成黄色警示色（#d29922）。条形图旁有刻度标记"10%、30%、50%、70%、90%"。条形图标签"上下文累积"（白色字号 22px，竖排）。

**画面底部：**
小注："读过的代码全部进入消息数组 → LLM 看到了完整上下文"（浅灰字号 24px，居中）。

光效：每张工具卡片有蓝色微光；时间轴线有流动光带；右侧进度条有累积填充感。

色彩：黑底 + 蓝色主调 + 绿色（成功结果）+ 黄色（容量警示）。

--- narration ---
面对一个陌生项目
Claude Code 开始系统性探索
先列出目录结构
再读 README 了解概况
然后读核心源码文件
搜索关键的类和函数
每一步都是一次工具调用
每次结果都追加到消息数组
十几轮下来
消息数组已经非常长
所有读过的代码都在上下文里


>>> 生成架构文档 #B16
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 综合输出图，分辨率 1080x1920。

整体风格：突出"上下文 → 综合 → 输出"的三段式视觉。

背景：深色（#0d1117）。

构图：画面纵向分为三个区域：上方"巨大上下文"、中间"LLM 综合"、下方"架构文档输出"。

**画面顶部 — 巨大的上下文堆栈：**
- 一个非常高的消息卡片堆叠（约 15 张迷你卡片堆在一起，颜色蓝紫交替），高度占画布 35%，宽度约 70%，居中。
- 堆栈整体发出蓝紫色微光（#58a6ff + #9d7aff 渐变），象征"满载信息"。
- 堆栈顶部小标签"消息数组：约 80% 满"（白色字号 24px）。
- 堆栈右上角一个小的"📚 = 整个项目的代码"标签（金色字号 22px）。

**画面中部 — LLM 综合：**
- 一个大型的 LLM 处理盒子（蓝色发光圆角矩形，宽度 60%、高度 16%），内部有一个发光的大脑图标 + "综合分析"字样（白色字号 30px 粗体）。
- 上方箭头从上下文堆栈指向 LLM 盒子，箭头粗壮、流光效果。
- 下方箭头从 LLM 盒子指向输出文档。

**画面下半部分 — 架构文档输出：**
- 一个清晰的文档卡片（白底/浅色背景模拟纸张感，但保持深色主题用 #161b22 + 白色边框）。
- 文档卡片宽度占 80%，高度占画布 30%。
- 文档内容（视觉清晰但伪文本）：
  - 顶部大标题"nanochat 架构文档"（白色字号 28px 粗体）。
  - 章节列表（每行带蓝色项目符号）：
    - "▸ 1. 项目概述"
    - "▸ 2. 核心模块"
    - "▸ 3. 数据流"
    - "▸ 4. 训练流程"
    - "▸ 5. 推理流程"
  - 章节文字下方有几行模拟正文（浅灰短横线占位）。
- 文档卡片左下角一个绿色对勾"✓ 生成完成"（#3fb950）。

**画面右侧（关键提示注释）：**
一行竖排文字"LLM 的理解来自上下文证据 — 不是来自本地长期记忆"（白色字号 26px，"上下文证据"用蓝色 #58a6ff 高亮，文字旋转 -90 度）。

光效：上下文堆栈整体发光；LLM 盒子脉冲；文档输出有成功光晕。

色彩：黑底 + 蓝紫上下文 + 蓝色 LLM + 干净的文档输出。

--- narration ---
当 LLM 收集到足够信息后
最后一次调用
它综合所有上下文中的代码
生成完整的架构文档
但要理解一个关键点
LLM 对项目的"理解"
来自上下文中的代码证据
不是来自某种本地长期记忆
上下文的质量
决定了输出的质量


>>> 场景二的启示 #B17
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 对比概念图，分辨率 1080x1920。

整体风格：清晰的左右/上下对比、概念可视化、教学级清晰度。

背景：深色（#0d1117）。

构图：画面纵向分为两个等高区块，强烈对比。

**上方区块 — 人类程序员：**
- 区块标题"🧑‍💻 人类程序员"（白色字号 36px 粗体，左对齐）。
- 主视觉：一个程序员的剪影（半身，浅灰色 #8b949e 简笔画风格）坐在电脑前。
- 程序员头部：一个发光的大脑图标（紫色 #9d7aff）+ 标签"大脑存储信息"（白色字号 26px）。
- 屏幕：显示几行抽象的代码符号。
- 一根箭头从程序员指向区块底部的输出"理解项目"（白色字号 30px，绿色对勾旁标）。
- 区块底部小注："信息内化为长期记忆"（浅灰字号 22px）。

**中间分隔线：**
横向虚线（#30363d），中间一个对比符号"VS"（金色字号 36px 粗体，圆形发光徽章）。

**下方区块 — AI Agent：**
- 区块标题"🤖 AI Agent"（白色字号 36px 粗体）。
- 主视觉：一个 LLM 盒子（蓝色发光），左侧延伸出一根非常长的"消息卷轴"（像卷起来的纸带，上面有色彩斑斓的文字行，象征消息数组），卷轴长度占画面宽度 70%。
- 卷轴标签"消息数组（每次都重读）"（白色字号 26px）。
- 一根箭头从 LLM 指向区块底部的输出"理解项目"（白色字号 30px，绿色对勾旁标）。
- 区块底部小注："信息存于上下文 — 调用结束就消失"（浅灰字号 22px，"调用结束就消失"用红色 #f85149 高亮）。

**画面顶部：**
大标题"两种'理解'机制"（白色字号 50px 粗体）。

**画面底部：**
强调结论"效果类似 但记忆机制完全不同"（白色字号 32px，粗体；"记忆机制"用蓝色 #58a6ff 高亮）。

光效：人类大脑紫色脉冲；AI 卷轴蓝色流光。

色彩：上方人类区块偏暖色（紫色大脑、暖灰）；下方 AI 区块偏冷色（蓝色卷轴）。

--- narration ---
这个场景揭示了一个本质
人类程序员用大脑存储信息
**AI Agent 用消息数组存储信息**
效果类似但机制完全不同
这也解释了一个常见现象
为什么有时 AI 会"忘记"之前说的
因为上下文太长被压缩了
丢失的信息就真的丢失了
而人类依靠长期记忆和理解迁移
这是两者最大的区别


>>> 场景三：自动修 Bug #B18
@enter: slide-left
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 场景标题卡，分辨率 1080x1920。

整体风格：电影海报式视觉冲击、突出"循环纠错"主题。

背景：深色（#0d1117），中心放射状的微光晕（#1a2332）。

构图：

**画面顶部小标签：**
"SCENE 03"（浅灰 #8b949e 字号 28px 等宽）。

**画面中央（主视觉）：**
一个戏剧性的"Bug → Fix"转化图：
- 中心位置：一个程式化的 Bug 图标（甲虫造型，红色 #f85149，约占画布宽 30%），带有一个粗红色的禁止符号 "⊘"。
- Bug 周围：一个发光的循环箭头圆环（蓝色 #58a6ff），象征不断的循环修复。
- 循环箭头终点：右下方一个绿色对勾图标"✓"（#3fb950），象征最终成功。
- 整个组合：从 Bug → 循环 → 对勾，形成动态的转化感。

**主视觉下方：**
- 主标题"自动修复 Bug"（白色字号 64px 粗体，居中）。
- 副标题"Agent 循环的真正威力"（蓝色 #58a6ff 字号 32px，居中）。

**画面底部：**
- 一行装饰性引导文字"当 AI 遇到错误时，它不会停下来"（浅灰 #8b949e 字号 26px，斜体，居中）。

**背景装饰：**
零散的代码错误符号（红色细小的"Error"、"NULL"、"undefined"等文字符号），分布稀疏，颜色非常暗（#3a1a1a），仅作氛围。

光效：Bug 图标有红色微光；循环箭头有流动蓝光；对勾有绿色成功光晕。

色彩：黑底 + 红色（错误）→ 蓝色（循环）→ 绿色（成功）的色彩叙事。

--- narration ---
第三个场景
展示 Agent 循环的真正威力
当 AI 遇到错误时
它不会停下来求助
而是自动分析、修复、重试
这是 Agent 和聊天机器人的本质区别


>>> 错误、修复、重试 #B19
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 详细流程图，分辨率 1080x1920。

整体风格：清晰的纵向流程图、突出"失败 → 重试"的循环。

背景：深色（#0d1117）。

构图：从上到下的流程节点，每个节点是一个圆角矩形卡片，节点之间用粗箭头连接。当出现失败时有红色循环箭头返回。

**节点 1（顶部）— 用户输入：**
- 卡片：绿色边条 + 深色背景，"👤 用户：编译我的项目"（白色字号 26px）。
- 高度 100px。

**↓ 箭头**

**节点 2 — LLM 决定调用 bash：**
- 蓝色边条卡片，"🤖 LLM → bash: make"（白色字号 24px 等宽字体）。
- 高度 100px。

**↓ 箭头**

**节点 3 — 编译失败！：**
- 红色边条卡片（#f85149 边条），背景深红微染（#3a1a1a），高度 120px。
- 内容："❌ 错误：undefined reference to 'foo'"（红色 #f85149 字号 22px 等宽字体）。
- 卡片右上角红色警告图标。

**↓ 箭头（多分支：往下走 + 一根曲线红色虚线箭头预备弯回 — 后面再画）**

**节点 4 — LLM 分析错误：**
- 蓝色边条卡片，"🤖 LLM → read_file: main.cpp"（白色字号 24px）。
- 副文字"分析错误源头"（浅灰字号 20px）。

**↓ 箭头**

**节点 5 — LLM 修复代码：**
- 蓝色边条卡片，"🤖 LLM → edit_file: 修复 foo 的实现"（白色字号 24px）。
- 高度 100px。

**↓ 箭头**

**节点 6 — 再次编译：**
- 蓝色边条卡片，"🤖 LLM → bash: make （重试）"（白色字号 24px）。

**↓ 箭头**

**节点 7（决策菱形）：**
- 一个菱形决策框（黄色边框 #d29922），"成功？"（白色字号 28px 粗体居中）。
- 菱形右侧标"否"，从右侧延伸一根弯曲的红色虚线箭头返回到节点 3 上方（形成循环），箭头标签"🔄 继续循环"（红色字号 22px）。
- 菱形下方标"是"，箭头继续向下。

**节点 8（最底部）— 编译成功：**
- 绿色边条卡片（#3fb950），"✅ 编译成功！"（白色字号 28px 粗体）。
- 高度 110px。
- 卡片有绿色光晕。

**画面顶部：**
小标题"修 Bug 的完整流程"（白色字号 38px 粗体，距顶 60px）。

**画面右侧（注释）：**
一个发光的循环箭头标识 + 文字"🔄 关键：自我纠错循环"（蓝色 #58a6ff 字号 26px，竖排或贴右侧）。

光效：成功节点有绿色光晕；红色失败节点有红色微光；循环箭头有红色流动光带；决策菱形有黄色脉冲。

色彩：黑底 + 多色节点（绿用户、蓝 LLM、红错误、黄决策、绿成功）+ 红色循环线突出"重试"。

--- narration ---
看这个流程
用户说"编译我的项目"
LLM 调用 make 命令
结果编译报错了
看到这个错误信息
LLM 不会放弃
它读取报错涉及的源文件
分析错误原因
调用编辑工具修复代码
然后再次编译
如果还是失败就继续循环
直到编译成功
或者确认无法解决才停下来


>>> 自我纠错的关键 #B20
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 强烈对比的概念图，分辨率 1080x1920。

整体风格：极简对比图、教科书级清晰、视觉冲击。

背景：深色（#0d1117）。

构图：画面纵向分为两个区块。

**上方区块（普通聊天）：**
- 区块顶部小标题"普通聊天 (无工具)"（浅灰 #8b949e 字号 30px）。
- 主视觉：一条简单的水平流程线 — 用户图标 → LLM 盒子 → 回答气泡 → 终点（一个简单的圆圈"END"）。整条线是一条直线，没有任何分叉或回环。
- 流程线颜色：单调灰色（#6e7681）。
- 标签"一去不回"（灰色字号 24px，斜体，标在流程线下方）。

**中间分隔区：**
一行小字"VS"（金色字号 36px 圆形徽章）。

**下方区块（Agent 模式）：**
- 区块顶部小标题"Agent 模式 (有工具)"（白色字号 30px 粗体）。
- 主视觉：一个动态的循环螺旋图 — 用户输入 → LLM → 工具 → 结果 → LLM → 工具 → 结果 → ... → 最终回答。这是一个层叠的螺旋形（俯视像漩涡），中心是输入，外围是输出，每一圈代表一次工具调用。
- 螺旋颜色：渐变的蓝色（#58a6ff），带流动光带，有发光效果。
- 螺旋上散布一些工具图标（小齿轮、扳手、文件），随路径分布。
- 标签"循环 + 反馈 = 自我纠错"（白色字号 26px 粗体，标在螺旋下方）。

**画面顶部：**
大标题"差别只在一件事"（白色字号 50px 粗体，距顶 60px）。

**画面底部（结论）：**
一行强调文字"**循环**"（巨大的蓝色字号 96px，居中，发光，等宽字体），下方小字"实现这个循环只需几行代码"（白色字号 26px）。

光效：上方流程线静态无光；下方螺旋有强烈的流动光效；底部"循环"二字有蓝色脉冲光晕。

色彩：上方暗淡灰色 vs 下方鲜活蓝色，形成"无聊 vs 智能"的对比叙事。

--- narration ---
对比一下两种模式
普通聊天是一问一答
用户提问、LLM 回答、结束
**Agent 模式**是一个循环
LLM 可以反复调用工具
看到结果后继续思考
这个循环给了 LLM 自我纠错的能力
而实现这个循环
只需要一个 while 加几行代码
架构本身极其简单
复杂的是工程细节
