>>> 开场：AI 编程的困惑 #B01
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 高质量科技感插画，分辨率 1080x1920。

整体风格：暗黑科技感、电影级氛围、玻璃拟态、霓虹光效、Anthropic 设计美学。

背景：深邃的纯黑底色（#0d1117），从中心向外渐变出微弱的蓝紫色环境光晕。背景上有非常稀疏、暗淡的神经网络连接线和小光点（#1a2332），作为氛围装饰，不抢主体。

主体：画面正中央是一个悬浮的终端代码编辑器窗口，玻璃质感半透明，圆角矩形（圆角约 24px），窗口边缘带柔和的蓝色霓虹外光（#58a6ff）。窗口尺寸约占画布 70% 宽、50% 高，垂直居中略偏上。

终端窗口内部细节：左侧有一列灰色行号；中间是彩色的代码片段，可见橙色关键字、绿色字符串、蓝色函数名、灰色注释（仅作视觉氛围，不需要可读）；底部有一个发光的青色命令行光标，正在闪烁。

终端窗口周围：飘浮着 3 个发光小图标，象征 AI 正在工作 — 左上方一个紫色发光的"大脑"图标，右上方一个蓝色齿轮图标（带旋转动感），右下方一个绿色对话气泡图标。每个图标都有柔和的光晕。

画面四角：散布几粒微小光点和半透明的数字粒子，营造数据流动感。

光效：主光源来自终端窗口本身，向外辐射柔光；前景清晰锐利，远景略微虚化（轻微景深）。

色彩：以深蓝黑为主调，蓝色（#58a6ff）和紫色（#9d7aff）作为强调，整体冷色调但不刺眼。

质感：4K 超高清、电影级渲染、精致细节、专业级摄影感。

避免：实际可读的文字、明亮饱和色、卡通风格、低质量纹理、人脸。

--- narration ---
你用过 AI 写代码吗
比如 **Claude Code**
你给它一个需求
它就开始读文件、改代码
运行测试、修复错误
一整套操作行云流水
看起来像一个有记忆的程序员
但 **LLM** 是没有记忆的
每次调用都是全新的开始
那它是怎么做到的？
今天我们从底层原理讲清楚


>>> LLM 的本质：无状态函数 #B02
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 极简科技概念图，分辨率 1080x1920。

整体风格：极简扁平化信息图、教科书级清晰度、科技蓝调、Apple Keynote 风格。

背景：纯净的深色（#0d1117），中心有非常微弱的圆形辐射光晕（#1a2332），其余空间留白干净。

主体构图（垂直居中）：

画面正中是一个大型的圆角矩形函数盒子，宽度约占画布 70%，高度约占 30%，圆角 32px。盒子边框是柔和发光的蓝色（#58a6ff），线宽 4px。盒子内部半透明、近乎中空，象征"无状态"。盒子内部居中有一个发光的"f"字符（数学函数符号），字号约 200px，蓝色（#58a6ff）半透明。

盒子左侧：一个文档图标（白色 #e6edf3，圆角矩形带横线代表文字），下面标注"输入文本"（白色字号 36px）。从文档图标向函数盒子延伸一根发光的蓝色箭头，带流动光效。

盒子右侧：对应的另一个文档图标，下方标注"输出文本"（白色字号 36px）。从函数盒子向这个文档图标延伸一根发光的蓝色箭头。

函数盒子下方约 100px：放置三个被红色斜杠划掉的图标，水平等距排列（间距 80px）：第一个是数据库图标（圆柱形）、第二个是内存条芯片图标、第三个是大脑图标。三个图标都是灰色（#6e7681），上面盖着粗红色的"禁止"斜杠（#f85149）。下方注释文字"无存储 · 无记忆 · 无状态"（白色字号 32px）。

画面顶部居中：标题"LLM 的本质"，字号 56px，粗体白色（#e6edf3），距顶 80px。

光效：主体盒子有柔和外发光，箭头有流动的光带效果。

色彩：黑底 + 白主体 + 蓝色强调 + 红色禁止符号。

质感：矢量图形、边缘锐利、专业信息图、4K 清晰。

避免：3D 透视、卡通风格、复杂背景、人物。

--- narration ---
我们从最底层讲起
**LLM** 本质上就是一个函数
给它一段文本输入
它返回一段文本输出
就这么简单
它没有硬盘
也没有数据库
没有任何地方存储记忆
每次调用之间完全独立
上一次说了什么它全忘了


>>> 聊天记忆的真相 #B03
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 信息图风格的概念示意图，分辨率 1080x1920。

整体风格：扁平化矢量信息图、教学级清晰、对比鲜明、专业 UI 设计。

背景：深色纯净背景（#0d1117），无装饰元素。

构图：画面纵向分为三个等高区块，从上到下依次展示三次 API 调用，每个区块占画布 1/3 高度。每个区块用细虚线（#30363d）分隔。

每个区块内部布局相同（左侧消息堆栈 → 箭头 → 右侧 LLM 盒子）：

**第一区块（顶部）— 第 1 次调用：**
- 左侧：1 个绿色聊天气泡（#3fb950），代表"用户消息"，宽度约画布 30%。
- 中间：粗箭头指向右，蓝色发光（#58a6ff）。
- 右侧：标有"LLM"的圆角矩形盒子（白色边框，蓝色发光），紧凑尺寸。
- 区块右上角小标签："Call #1"（蓝色字号 28px）。

**第二区块（中部）— 第 2 次调用：**
- 左侧：2 个气泡堆叠 — 上面是绿色用户气泡（变小），下面是蓝色助手气泡（#58a6ff），代表"上轮对话 + 新问题"。
- 中间：箭头（比第一区块略粗，颜色更亮）。
- 右侧：同样的 LLM 盒子。
- 区块右上角："Call #2"。

**第三区块（底部）— 第 3 次调用：**
- 左侧：4 个气泡堆叠 — 用户绿、助手蓝、用户绿、助手蓝，代表全部历史。
- 中间：箭头更粗、更亮，明显比上方两个壮实。
- 右侧：LLM 盒子。
- 区块右上角："Call #3"。

视觉重点：三个区块从上到下，左侧的消息堆栈逐渐变高变多，箭头逐渐变粗，形成"输入越来越长"的强烈对比。每个区块的 LLM 盒子大小不变 — 强调 LLM 本身没有变化，是上下文在累积。

画面右侧（贯穿三个区块的纵向标注）：一根从上到下的标注线，旁边写着大字"每次都重发完整历史"（白色 #e6edf3，字号 40px，粗体），文字旋转 -90 度沿纵向排列，颜色为蓝色（#58a6ff）。

画面顶部：标题"对话记忆的真相"，字号 52px，粗体白色，距顶 60px。

光效：每个气泡和盒子都有柔和投影，箭头带发光效果。

色彩：黑底、白线条、蓝色（#58a6ff）和绿色（#3fb950）作为消息颜色区分。

避免：照片质感、3D 效果、复杂纹理。

--- narration ---
那聊天时的"记忆"呢
真相非常简单
**每次调用都发送完整对话**
第一轮只发你的提问
第二轮发上轮问答加新问题
第三轮发所有历史加新问题
就像每次都重新读一遍聊天记录
这就是所谓"记忆"的真相
不是 LLM 记住了什么
而是我们每次都重复告诉它


>>> 工具调用：LLM 的双手 #B04
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 流程示意图，分辨率 1080x1920。

整体风格：科技感矢量信息图、流程清晰、突出"指令-执行-反馈"的循环关系。

背景：深色（#0d1117），中间有非常微弱的环形光晕暗示循环。

构图：画面从上到下三个步骤，垂直流动，用粗箭头连接。整体形成一个"⤵ ⤴"循环回路。

**步骤 1（顶部）— LLM 输出指令：**
- 左侧：LLM 盒子（圆角矩形，蓝色发光边框，标注"LLM"白色字号 40px）。
- 右侧：从 LLM 盒子向右延伸出一张代码卡片，背景深灰（#161b22），圆角 16px，有细微边框光。卡片内显示 JSON 风格的伪代码（用代码块视觉呈现，无需精确可读）：可见到形如 `{"tool": "..."}` 的形状，关键字蓝色高亮、字符串绿色高亮。卡片标签："工具调用指令（仅是文本）"（黄色字号 28px）。
- 步骤标号"①"在左上角，圆形徽章蓝色背景白字。

**步骤 2（中部）— 外部程序执行：**
- 中央：一个齿轮图标（大号，蓝色发光，#58a6ff），周围环绕表示动作的小图标 — 左侧"文件写入"图标，右侧"终端运行"图标。
- 上方箭头从步骤 1 的代码卡片指向这个齿轮，箭头标签"传入指令"。
- 下方箭头从齿轮指向步骤 3，标签"执行 + 收集结果"。
- 步骤标号"②"。
- 注释文字："Claude Code 程序解析 JSON 并真正执行"（白色字号 26px，紧贴下方）。

**步骤 3（底部）— 结果回传给 LLM：**
- 左侧：一个深灰色结果卡片，显示成功/失败图标 + 简短文本占位，标注"工具结果"。
- 右侧：箭头指回最上方的 LLM 盒子（这条箭头是弧形/曲线，从底部沿右侧绕回顶部），形成循环视觉。箭头颜色：渐变蓝紫，发光。
- 步骤标号"③"。

画面右侧空白处（垂直方向）：一行竖排的强调文字"LLM 不会执行任何操作 它只是说要执行什么"（白色字号 32px，粗体，重点字"说要执行什么"用蓝色 #58a6ff 高亮）。

画面顶部居中：标题"工具调用 = LLM 的双手"，字号 48px，粗体白色。

光效：循环箭头有流动光效，象征数据在循环中传递；齿轮图标有微微旋转感（光效暗示）。

色彩：黑底 + 蓝色（#58a6ff）主调 + 黄色（#d29922）作为关键标签 + 灰色（#161b22）作为卡片底色。

避免：照片真实感、人物、过度装饰。

--- narration ---
但聊天只能生成文字
怎么操作电脑呢
答案是 **工具调用**
LLM 不直接执行任何操作
它只输出一段结构化的指令
比如这样的 JSON 文本
"请写入这个文件"
本质上还是在生成文字
真正执行的是外部程序
程序拿到指令完成动作
再把结果写回消息数组
LLM 看到结果继续思考


>>> System Prompt：Agent 的说明书 #B05
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 解构图风格，分辨率 1080x1920。

整体风格：技术蓝图风格、分层解析、清晰展示一次 API 调用包含的所有内容。

背景：深色（#0d1117），有非常细微的网格纹理（#1a2332），像技术文档底纹。

主构图：画面中心是一个"API 调用的解剖图"，分三层从上到下堆叠，每层是一个矩形卡片，宽度都占画布 80%，居中对齐。三层之间有细线连接。

**第一层（顶部，最大、最强调）— System Prompt：**
- 卡片背景深棕黄色（#3d2c0e），左侧 8px 粗的金色边条（#d29922）。
- 卡片高度约 480px。
- 卡片标题"System Prompt"（金色 #d29922，字号 44px，粗体），紧贴左上角。
- 卡片副标题"几千 token 的预设指令"（白色字号 28px）。
- 卡片内容：3-4 行内嵌的小标签，每个标签是浅金色填充的圆角矩形，标签文字（白色字号 26px）— "你是 Claude Code"、"环境: macOS / bash"、"工具: Read/Write/Bash..."、"规则: 不写无关代码..."。这些标签错落排列，仿佛文档里的关键条目。

**第二层（中间）— Tools 定义：**
- 卡片背景深蓝色（#1f2d3d），左侧 8px 粗蓝色边条（#58a6ff）。
- 卡片高度约 280px。
- 标题"Tools 定义"（蓝色 #58a6ff，字号 40px）。
- 内部展示 3 个并排的小工具卡片（深色底 #0d1117 + 蓝色边框）：每个卡片有一个图标 + 工具名 — 文档图标"read_file"、笔图标"write_file"、终端图标"bash"。

**第三层（底部）— Messages 数组：**
- 卡片背景深灰（#1c2128），左侧 8px 白色边条（#e6edf3）。
- 卡片高度约 240px。
- 标题"Messages 数组"（白色字号 40px）。
- 内部展示一列堆叠的小气泡（绿/蓝交替），代表对话历史。

**底部连接线 + LLM 盒子：**
三层卡片下方延伸出三根汇聚的细线，连接到画面最底部的一个发光"LLM 处理器"盒子（蓝色发光圆角矩形，里面有一个 CPU/芯片图标）。盒子下方箭头指向画面外，标签"输出"。

画面顶部居中：标题"一次 API 调用的全貌"，字号 50px，粗体白色，距顶 80px。

光效：三层卡片有柔和投影；System Prompt 层有金色环境光最强烈；底部 LLM 盒子有蓝色脉冲发光。

色彩：黑底 + 金色（#d29922 System Prompt）+ 蓝色（#58a6ff Tools）+ 白色（Messages）形成清晰的三色区分。

避免：照片质感、卡通、过多装饰。

--- narration ---
还有一个关键组件
叫 **System Prompt**
它是每次 API 调用都附带的指令
告诉 LLM 它是谁
有哪些工具可以用
应该遵循什么规则
还包含环境信息
比如操作系统类型
就像给新员工的岗位说明书
**Claude Code** 的 System Prompt
长达几千个 token
定义了它所有的行为规范


>>> Agent 循环：核心引擎 #B06
@enter: fade-up
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 循环引擎概念图，分辨率 1080x1920。

整体风格：科技感矢量插画、强调循环动感、霓虹光效、未来感。

背景：深邃黑色（#0d1117），中心位置有一个柔和的圆形蓝色光晕（直径约画布 80%），从中心向外淡出。

主构图（画面正中）：一个大型的圆形循环图，直径约占画布 75%，垂直居中。

**循环圆环：**
环形为蓝色发光圆环（#58a6ff），线宽 12px，带流动光效。环上有 4 个等距分布的节点（圆形发光球），分别在 12 点、3 点、6 点、9 点位置：
- **顶部（12 点）"思考"** — 大脑图标（紫色 #9d7aff），下方标签"思考"白色字号 32px。
- **右侧（3 点）"行动"** — 扳手/工具图标（橙色 #ff7b72），标签"行动（调用工具）"。
- **底部（6 点）"观察"** — 眼睛图标（绿色 #3fb950），标签"观察（看结果）"。
- **左侧（9 点）"决策"** — 灯泡图标（黄色 #d29922），标签"决策（继续 or 停止）"。

环与环之间用箭头连接，箭头指向顺时针方向，箭头有流动的光带动画感。

**循环中心：**
正中央有一个发光的"无穷符号"∞（蓝色 #58a6ff），字号 120px，柔和光晕。下方一行小字"Agent Loop"（白色字号 36px，粗体）。

**外部连接：**
- 画面顶部：从外部一个绿色用户图标（人形剪影）延伸一根箭头，进入循环顶部节点，标签"用户请求"。
- 画面底部：从循环底部延伸一根箭头出去，连接到一个绿色的对勾/完成图标，标签"任务完成"。

**底部强调：**
循环图下方约 80px：一行强调文字"本质上：一个 while 循环"（白色字号 38px，粗体；"while 循环"用蓝色 #58a6ff 高亮，等宽字体）。

画面顶部居中：标题"Agent 循环：核心引擎"，字号 50px，粗体白色，距顶 60px。

光效：循环箭头有沿环流动的光带；中心无穷符号有脉冲光晕；四个节点的图标各有自己颜色的微光。

色彩：黑底 + 蓝色主循环 + 四节点不同颜色（紫橙绿黄）作为阶段区分。

质感：矢量插画、4K 锐利、精致光效。

避免：照片真实感、人物面部、过度复杂。

--- narration ---
把这些拼在一起
就是 **Agent 循环**
用户发出请求
LLM 思考后决定调用工具
程序执行工具返回结果
LLM 看到结果继续思考
可能再次调用工具
也可能认为任务已完成
这个不断循环的过程
就是 AI 编程的核心引擎
说穿了它就是一个 while 循环
没有任何神秘的地方


>>> 五大组件总结 #B07
@enter: fade
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 信息图卡片堆叠风格，分辨率 1080x1920。

整体风格：扁平化设计、清晰的卡片信息图、模块化、教科书级整洁。

背景：纯净深色（#0d1117）。

构图：画面从上到下依次排列 5 张组件卡片，每张卡片宽度占画布 88%，居中对齐，卡片之间间距 32px。每张卡片以"图标 + 标题 + 副标题"的横向布局呈现。

每张卡片规格：
- 高度约 240px
- 圆角 20px
- 背景深灰（#161b22）
- 左侧有一条 12px 粗的彩色发光边条（颜色因卡片而异）
- 左侧大图标占卡片高度 60%，居中
- 右侧文字区域：上方大字标题（字号 44px 粗体白色），下方副标题（字号 28px 浅灰 #8b949e）

**卡片 1（最上方）— LLM：**
- 边条颜色：蓝色（#58a6ff）
- 图标：大脑图标（蓝色发光）
- 标题："LLM"
- 副标题："负责思考与决策"

**卡片 2 — System Prompt：**
- 边条颜色：金色（#d29922）
- 图标：卷轴/文档图标（金色）
- 标题："System Prompt"
- 副标题："定义角色、工具、规则"

**卡片 3 — 工具：**
- 边条颜色：橙色（#ff7b72）
- 图标：扳手 + 齿轮组合图标（橙色）
- 标题："工具"
- 副标题："让 LLM 操作真实世界"

**卡片 4 — 消息数组：**
- 边条颜色：紫色（#9d7aff）
- 图标：列表/堆叠气泡图标（紫色）
- 标题："消息数组"
- 副标题："充当对话记忆"

**卡片 5（最下方）— Agent 循环：**
- 边条颜色：绿色（#3fb950）
- 图标：循环箭头 ∞ 图标（绿色发光）
- 标题："Agent 循环"
- 副标题："把一切串联起来"

**视觉串联：**
五张卡片左侧有一条非常淡的纵向连接线（#30363d，1px 细虚线），把它们的彩色边条串成一条"组件链"。

**画面顶部：**
大标题"AI 编程的全部组件"（字号 52px 粗体白色，距顶 60px），副标题"就这五样，没了"（字号 32px 浅蓝 #58a6ff）。

光效：每张卡片有微妙的投影；左侧彩色边条有自身颜色的柔和发光。

色彩：黑底 + 五种区分色，彼此不冲突，整体协调。

避免：3D 效果、照片质感、复杂纹理。

--- narration ---
总结一下
AI 编程只需要五样东西
**LLM** 负责思考和决策
**System Prompt** 定义角色规则
**工具** 让 LLM 操作真实世界
**消息数组** 充当对话记忆
**Agent 循环** 把一切串联起来
没有魔法
没有神秘的"智能体"
就是一个循环不断调 API
听起来简单？
接下来我们看真实场景


>>> 过渡：进入实战 #B08
@enter: zoom-in
@exit: fade
@visual: image

--- visual ---
竖屏 9:16 章节过渡卡片，分辨率 1080x1920。

整体风格：电影级章节标题卡、极简、有冲击力、留白充足。

背景：纯净深色（#0d1117），背景上隐约可见非常淡的代码符号水印（{}, ();, => 等），颜色非常暗（#1a2332），分布稀疏作为氛围装饰。

主构图：画面垂直居中区域。

**主标题（画面正中）：**
- 大字"实战拆解"（粗体白色 #e6edf3，字号 140px，字间距加宽）。
- 标题正下方：一条 6px 粗的蓝色发光横线（#58a6ff），从左向右延伸，宽度约画布 50%，居中。

**主标题上方约 100px：**
- 小标签"PART 02"（蓝色 #58a6ff，字号 32px，等宽字体，字母间距加宽）。

**主标题下方约 120px（章节列表）：**
三行场景标题，每行格式："数字编号 + 场景名称"，垂直堆叠：
- "01　用 C++ 写 Hello World"（数字 01 是大字 64px 蓝色发光，场景名 36px 白色）
- "02　分析开源项目架构"
- "03　自动修复 Bug"
每行之间间距 28px。

**画面底部约 120px 处：**
一行装饰性引导文字"接下来，我们追踪每一次 LLM 调用"（浅灰 #8b949e，字号 28px，斜体）。

光效：主标题有非常微妙的发光；副标题装饰线带流动光感。

色彩：黑底 + 白色主体 + 蓝色（#58a6ff）作为唯一强调色。

质感：电影级排版、Apple Keynote 美学、极简主义。

避免：复杂插画、卡通、过多元素。

--- narration ---
接下来我们用三个真实场景
追踪 AI 编程的每一步
看看每次 LLM 调用
到底输入了什么
又输出了什么
